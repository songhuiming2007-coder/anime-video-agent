# CLAUDE.md — anime-video-agent

本文件写本项目特有的**工程约定**，任何人拿这个仓库都适用，进 git。

只对这台机器 / 这个账号成立的事实——盘符、系统盘余量、账号播放数据、本项目在我这里的
优先级——在 `CLAUDE.local.md`，**不进 git**。
两者分开的理由很实际：混在一起，别人分不清哪条是原则、哪条是「他家的情况」。

## 项目定位

用 agent 流水线批量生产动漫二创视频（人物志 / 结合动漫的生活思考）。

**这是实验项目，不是本职工作。** 它的优先级低于主线（本机的具体情况见 `CLAUDE.local.md`）。

因此成功标准是**流水线本身**，不是内容表现：

- 主要交付物 = 一套可复用的 agent 工程系统（本地索引 + 语义检索 + 多阶段流水线 + 自动质检），可写进简历
- 内容数据（播放、涨粉）= 副产品与验证信号，不是目标
- 判断任何取舍时问：这件事让**流水线**更好，还是只让**这一期视频**更好？后者一律降优先级

## 硬约束（违反即视为系统缺陷）

**每期人类投入 ≤ 10 分钟。**（剪映微调环节已砍除，见 ADR-0001）

超预算不是"这期多花点时间"，是流水线有 bug。处理方式是**改流水线或砍掉该环节**，绝不是加时间。每期必须在 `meta.md` 记录实际人类耗时；连续 3 期超预算 → 停止产出，回头修流水线。

**建设期总投入上限：3 周，每周 ≤ 6 小时。**

到期未跑通端到端 → 停掉项目，不续投。实验要有止损线。

**禁止在本项目上做无限打磨。** 画质、转场、文案润色的边际收益极低，一律不碰；时间只花在让流水线更自动、更可靠上。

## 文案由 Agent 全权生成

**这是一个知情的取舍，不是疏漏，不要在会话中反复劝说改回人工写稿。**

理由：人工写内核约 30 分钟/期，会直接吃掉学习时间，这个代价高于文案质量下降带来的损失。接受内容上限较低，换取时间成本接近零。

但**文案生成必须是可替换模块（seam）**，切换只改配置、不改流水线：

```
config/script-mode = auto      # 当前默认：agent 从选题直接生成全稿
config/script-mode = from-core # 备用：人写 200-400 字内核，agent 扩写
```

`from-core` 模式的接口先预留、不实现。将来若某期题材值得亲自写，改一行配置即可。

## 存储约定

仓库本身只有纯文本（代码、skills、配置、文档），全部进 git。
所有大件——片源、字幕、索引、音色模型、每期产物——都在 `data/` 下，**不进 git**。

`data/` 放在哪由使用者定，两种放法都支持：

| 放法 | 何时用 |
|---|---|
| **符号链接 → 外置盘** | 系统盘装不下。一部番的片源就有几十 G |
| **真实目录** | 系统盘够大，`clone` 下来 `mkdir data` 即可 |

**代码中一律使用 `./data/...` 相对路径**，不要硬编码卷路径——它通常含空格，且卷名会变。
`data` 这个名字是唯一的绑定点。

**脚本绝不自动创建 `data/`，不可达就立刻报错退出。** 这条是硬的，理由不是洁癖：

- 符号链接指向未挂载的卷时，`mkdir -p data/library` 会在 `/Volumes/` 里建出实体目录，
  几十 G 静默写进系统盘，而且**盘插上之后还看不见它**（挂载点被占，系统改挂到 `卷名 1`）
- 「目录不存在就建一个」这种便利，代价是把「配置错了」变成「跑起来了但东西在别处」

建骨架是显式动作：`./pipeline/preflight.sh --init <目标目录>`，
建完打印落在哪块盘上。作者本机的具体位置见 `CLAUDE.local.md`。

**HuggingFace 缓存（2026-07-29 迁移）：**

| 谁 | 缓存位置 | 由什么保证 |
|---|---|---|
| 本项目 | `data/models/hub/` | `pipeline/paths.py` 设 `HF_HOME`，顺序错了会报错 |
| 机器上其他工具 | 外置盘上的 `hf-cache/` | `~/.cache/huggingface` 已改成指向它的符号链接 |

代价是**外置盘没插时，机器上任何用 HuggingFace 的工具都会报错**——这是有意的，
宁可当场失败也不要静默往系统盘灌几个 G。撤销方法见 `CLAUDE.local.md`。

## 目录结构

```
anime-video-agent/                    # Mac，git 仓库
├── CLAUDE.md                         # 工程约定，进 git
├── CLAUDE.local.md                   # 本机与本账号，不进 git（有 .example 模板）
├── data -> 外置盘 或 实体目录          # 见「存储约定」
├── pipeline/                         # 各阶段脚本，每个可独立运行
├── skills/                           # 项目内 agent skills
├── tests/                            # 纯函数测试
├── config/
│   ├── project.json                  # 跨模块共用的可调参数
│   ├── voice.json                    # 配音引擎与音色，Phase 0 定死
│   └── bgm.json                      # 按番分组的曲目表 + 实测响度
└── docs/adr/                         # 只记难以回头的决策

data/
├── library/
│   ├── raw/                          # 片源
│   ├── subs/                         # 字幕（提取 / ASR 生成）
│   ├── index/                        # 台词向量索引
│   ├── bgm/                          # 从 CD 镜像分出来的 instrumental 轨
│   └── notes/                        # 每部番的笔记，选题来源
├── models/                           # 全部模型权重，HF_HOME 指向这里
│   └── local/                        # 直下的平铺模型目录
├── voice/                            # 参考干声 + 试音产物
└── episodes/
    └── YYYY-MM-DD-<番>-<主题>/
```

## 两层流程

混淆这两层是本项目最容易犯的错误。

### Phase 0：一次性建设

片源入库 / 字幕索引 / 音色 / BGM 曲目表 / 番剧笔记。做一次，之后只维护。
**不要把 Phase 0 的动作写进每期循环**——每期重跑既烧 token 又导致风格漂移。

> 早期设计里还有一项「参考视频拆解 → `config/style-guide.md`」，**从未建过，
> 也没有任何代码读它**，2026-07-30 删除。留着不执行的条款会让整份文档失去权威。

**一部番的 Phase 0 成本是固定的，只有摊到多期上才划算。**

春物这一部的实测账：下载几十 G、37 集要重下、逐集验完整性与对轴、建索引、写分集笔记、
定 BGM 曲目表——这是几小时的量，而且这几小时跟做 1 期还是做 10 期无关。

所以：

- **一部番要榨干再换。** 现有番的选题池枯竭之前不开新番。判据是「还提不提得出带得动张力的选题」，
  不是「做腻了」
- **番剧笔记要写厚**。它不是某一期的附属品，是这部番所有期共用的资产。多花半小时写笔记，
  省的是后面每一期的查证时间
- **换番的决定要显式做**，并在 `meta.md` 记一句为什么——不要不知不觉地漂到下一部番

#### Phase 0 必须一次做完全集，做不完就不许出片（2026-07-30 增补）

**开一部番的坑之前，全集正片必须先入库、笔记必须先写全。**
「先索引几集试试水，边做边补」是禁止的做法。

判据是三条数字必须相等，任一条落后就不许进每期循环：

| | 命令 | 春物踩坑时 |
|---|---|---|
| 完整片源 | `ingest intact` | 41 ✅ |
| 索引集数 | `ls data/library/index/<番>_*.npy \| wc -l` | **6** ❌ |
| 笔记集数 | 分集速查表行数 | **31** ⚠️ |

整季入库有专门的命令，三步合一，不许拆开只跑其中一两步：

```bash
python -m pipeline.ingest phase0 <该季所有 mkv> --anime <番> --season <N>
```

**为什么这条要单独立规矩：素材边界缩水是唯一一种「越用越不觉得有问题」的失败。**

2026-07-30 发现片源 41 集早已下完，索引却停在 6 集——笔记里那句「完整视频只有 3 集」
是 7-29 的真实情况，后来补下完了，但没人回头改笔记，也没人回头重建索引。
中间每一期都在 6 集的池子里挑素材、在 31 集的笔记里找锚点，**全程没有任何报错**：
检索照常返回 Top-K，分数照常在阈值以上，成片照常渲染出来。

它的症状会被误诊成别的问题。上一期封面九张候选里挑不出一张主角占主位，当时归因于
「字幕检索有偏置」——偏置确实存在，但真正的放大器是池子只有应有的七分之一。
**在一个假的素材边界里，你调什么参数都是在调错的东西。**

所以判据不能是「检索得到东西吗」，必须是「集数对得上吗」。前者永远为真。

**字幕来源是硬规则，违反会静默毁掉整条流水线：**

> 字幕的时间轴必须是照你手上那个视频文件打的。允许三种来源，**只有三种**：
>
> 1. ffmpeg 从该文件提取内封
> 2. WhisperX / mlx-whisper 对该文件转录
> 3. **与该文件同属一个发布、文件名逐一对应的外挂字幕**，且通过下方的对轴校验
>
> **永不用第三方字幕文件配第三方视频文件。**

跨版本配对（BD 版字幕 + TV 版视频等）的差异是中间插入与删除，不是恒定偏移，加 offset 修不好。
而帧级精确截取是整条渲染链路的前提，前提一塌，视频照常渲染出来，只是全程画面对不上话——
失败是静默的，没有任何报错。

集间结构差异（某集无 OP、末集无 ED）不构成问题：只要字幕的轴出自该集视频，
它本来就是按该集实际结构打的。

#### 第 3 种来源的判据（2026-07-29 增补）

原规则只写了内封与 ASR 两种，遇到「压制组把字幕作为独立 `.ass` 随片发布」就没有条款可依——
这既不是第三方字幕，也不是跨版本配对，是规则当初没预料到的情况。春物三季全是这种发布形式。

采纳它的理由：这种字幕的轴本来就是照这个文件打的，**比 ASR 更准**
（ASR 实测有 0.34s 系统性偏早，外加同音字错误），而且几秒钟就能入库，不用跑一小时转录。

**但「同一发布」这个前提必须机器验证，不许靠肉眼看文件名。** 判据两条，缺一不可：

1. 字幕文件与视频文件**同目录、主文件名相同**，只有扩展名与语言后缀不同
2. **过 `pipeline/ingest.py verify` 的对轴校验**：对该视频抽一段做 ASR，与字幕在同一时间
   区间内的文本比对，字符错误率低于阈值才算对上

第 2 条是重点。文件名对应只能证明「打包时放在一起」，证明不了轴没错；
真正能证伪的只有拿视频自己的声音去对。**没跑过 verify 的外挂字幕不许进索引。**

#### 片源完整性的判据（2026-07-29 修订）

对轴校验的前提是片源本身完整。**没下完的视频，ffprobe 照样读得出时长**——BT 没下完的文件
是稀疏文件，文件头完好，只有解到空洞处才出错。2026-07-29 就这么误判过一次：38 集里 16 集
「通过」了文件头检查，实际全部残缺。

判据是拿 ffmpeg **只解复用地走一遍全片**，任何报错即判残缺：

```bash
python -m pipeline.ingest intact <视频>...
```

**不要用「实占块 ÷ 逻辑大小」判完整。** 那是本条最初的做法，2026-07-29 推翻。
推翻依据是拿 qBittorrent 的 piece 级校验（每片 SHA-1，密码学强度）当真值，对 89 个 mkv
逐个比对：

| 判据 | 漏放残缺 | 误杀完整 | 耗时 |
|---|---|---|---|
| 实占块 ÷ 逻辑大小 ≥ 99% | **14 / 72** | 0 / 17 | 瞬时 |
| `ffmpeg -c copy -f null -` | **0 / 72** | 0 / 17 | 0.88s/文件 |

块比例最大高估 83pp——一个真实只下了 16.7% 的文件读出 100.1%，另有一个读出 124.4%
（比文件本身还大）。成因是 piece 校验失败后下载器只在逻辑上把该片标回缺失，
**磁盘上那些字节还留着**，块数不会退回去；碎片化的稀疏文件在 APFS 上还有额外 extent 开销。
最初那句「稀疏文件的空洞不占块」在原理上没错，错在没考虑写过又作废的数据。

块比例降级为**前置快筛**：它说「没下完」可信（不会凭空少块），说「下完了」不可信。
快筛先毙掉的文件不必再跑 ffmpeg。

**不要让管道去查下载器的 API。** piece 校验确实是最强的证据，但种子一删就没了，
而且会把视频管道绑死在一个下载客户端上。判据必须自包含。

### 每期循环

一期一目录，产物文件即状态：看目录里有哪些文件就知道进行到哪，不需要额外状态记录。

| 序号 | 阶段 | 谁 | 产物 |
|---|---|---|---|
| 01 | 选题（周批量） | Agent 提 → 人勾选 | `01-topic.md` |

**`01-topic.md` 必须携带 `模式`（驳论/立论）与 `张力`（一句话：靶子或疑点）。**
张力是整条流水线里唯一真正的编辑判断，而它只有一句话——所以它必须在这一步定死并经人批准，
不能留到写稿阶段由 agent 自行发明。写稿只校验、不更换。

| 02 | 全稿 + 分镜 | Agent | `02-script.md` |
| 03 | 配音 | Agent | `03-audio/` |
| 04 | 素材检索 | Agent | `04-clips.json` |
| 05 | 时间码抽检 | 人 | `04-clips.approved.json` |
| 06 | 渲染成片 | Agent | `05-final.mp4` |
| 07 | 质检 | 机器 | `06-check.log` |
| 08 | 封面与标题 | Agent | `07-cover/`、`07-titles.md` |
| 09 | 发布 | 人 | `meta.md` |

**顺序不可交换：必须先配音拿到每段真实时长，再排画面轨。** 反过来做全程对不齐。

排画面要按每段**音频实测时长**填，不是按字数估算。字数估算与实际语速的偏差每段几百毫秒，
单看无所谓，二十段攒下来就是几秒——而且是渐进错位，越到后面画面与口播差得越远，
**全程不报错**。

> **编号一度与真实顺序错位**（2026-07-30 修正）：文件名曾经是 `03-clips` / `04-audio`，
> 而实际执行是先配音后排片。表、正文、README、文件名四处有两处对两处错，
> 谁也说不清哪个是准的。编号就是给人看顺序的，错位的编号比没有编号更糟。

人类只在 **05 和 09** 出现，合计应 ≤ 10 分钟。**不要往流程里加回任何需要人工操作 GUI 的环节。**

**第 08 步的分工是硬的：机器筛掉不能当封面的帧，不假装能判「哪张有张力」。**
清晰度、黑场过曝、重复镜头——这些有明确判据，机器筛。
「吸引眼球」没有判据，机器给 9 张候选**且不排名**，人在第 09 步点一张。
把审美判断包装成机器判据，等于把一个说不清的标准写死在代码里，比不做更糟。

这条是踩出来的：2026-07-29 给候选加过两版打分，两版都把正确答案压掉了。
根因是**拉普拉斯方差测的是「画面里有多少细节」，不是「画面有多好」**——
杂乱背景细节最多，按它排专挑最杂乱的画面。清晰度是准入门槛，不是排序依据。
**一个量能用来剔除，不代表它能用来排序。**

## 清理约定

**时机：人拍板最终版之后可以清理。清理前每次主动问，不许自己删。**

「拍板」指人明确选定了这一期的成片、封面、标题。在那之前所有中间产物都不动——
挑完九张封面候选不等于定稿，人完全可能说「都不行，重挑」，那时缓存正好还要用。
**脚本不许把「什么时候算定稿」这个判断替人做**，它只负责报出体积和可清的路径。

清理本身命中全局 CLAUDE.md 的红线（删除文件必须先问），所以是「报清单 → 人点头 → 删」，
没有例外，也不因为之前授权过同类操作就顺手删下一批。

| 类别 | 例子 | 定稿后 |
|---|---|---|
| 编号产物 | `01-topic.md` … `08-cover.jpg`、`meta.md` | **永久留**，产物文件即状态 |
| 成片与配音 | `05-final.mp4`、`03-audio/` | 留。改稿重渲要用，一期几十 M 不值得省 |
| 中间缓存 | `07-cover/raw/`（841 帧）、`sheet-*.jpg` | 问过就删，重跑约 2 分钟可重生成 |
| 交接副本 | `04-thumbs/`、`04-review.html`、`04-clips.json` | 问过就删，`approved` 版留 |
| 会话临时文件 | 试听 wav、抽帧 jpg、验证脚本 | 问过就删，结论应已写进代码注释或 ADR |

**判据是「重生成要多久」，不是「占多大」。** 几十秒能重来的一律可清；
不可重生成的（片源、参考干声、番剧笔记、BGM 分轨）一律不清——
后者哪怕占几百 M 也留着，它们是 Phase 0 资产，重做要几小时。

## 内容参数

以下每一条都来自自己账号的实测复盘，不是拍脑袋的假设。**具体播放数字在
`CLAUDE.local.md`——那是账号数据，不进 git，而且会过时。** 这里只留规则和它的方向性依据。

改动任何一条前先看新数据是否已经推翻它；推翻了就**先改这条规则、再改实践**。

**平台：抖音优先，B 站同步分发。**
同内容抖音播放高一个量级；B 站退回的选题在抖音仍能存活。

**画幅：16:9 横屏。已决策，不再讨论。**
番剧构图是为 16:9 设计的，裁切竖屏会切掉两侧破坏画面。且现有最高播放本就是横屏拿到的，
数据不支持竖屏更优的假设。

**时长：2–4 分钟。**
两平台数据一致地呈现时长与播放量负相关；7–8 分钟的那几条在两个平台都垫底。

**选题标准：情感共鸣密度 × 番剧认知度。不是热度榜排名。**
情感/恋爱向远优于热血向——认知度最高的热血番反而垫底，说明认知度本身不是主导变量。

**账号垂直度是硬约束。** 技术类、体育类的播放接近于零。
不相关题材会稀释账号标签，一律不发。

**素材配比：动画 ≥ 80%，漫画 ≤ 20% 作补充。**
受众预期是动态画面，纯静态会像幻灯片；且现有最高播放就是动画素材。
漫画用于动画里找不到对应画面时的补位，以及需要静止凝视的段落。

**标题用金句式，不用论文式。** 两端的实例见 `CLAUDE.local.md`。

**政治议题不进自动管道。**
实测过一条政论，B 站被退回仅自见、抖音明显限流。
Agent 不掌握中文平台的审核边界，自动生成政论稿的过审风险显著高于人工。
想写政论走人工通道手发，不占用管道。

## Agent 行为准则

**诚实失败优于凑合交付。** 拿不到关键素材、配音反复不自然、检索无匹配——立即停下来报告，不许降低标准硬交。这是本项目最重要的一条。

**不许硬塞不匹配的画面。** 留白远优于画文不符。检索落空时按「查询阶梯」逐级换问法，
三级都不及格就写 `"status": "no_match"` 并让渲染硬失败，交人处理——**不自动降级**。
理由与实测见下方「检索落空的处理：查询阶梯」。

**每阶段产物落盘。** 人机交接一律走文件，不走对话上下文。任何一步失败都能从中间接上重跑，不用整条重来。

**新装依赖一律装在项目目录内**（`uv` 虚拟环境），不污染全局。

## 检索落空的处理：查询阶梯（2026-07-30 定）

检索无匹配时**加大查询力度，逐级换问法，直到有片段的分数够格**。
门槛 `NO_MATCH = 0.45` 全程不动——加大的是找的力度，不是放低的标准。

| 级 | 用什么当查询 | 何时启用 |
|---|---|---|
| 1 | `查询` 字段 | 总是 |
| 2 | `备选` 字段 | 第 1 级 top-1 < 门槛 |
| 3 | 该段 `配音` 原文 | 第 1、2 级都 < 门槛 |

**阶梯是「只救不比」，不是「三种问法取最高分」。** 上一级够格就到此为止，
绝不因为下一级分数更高而换掉。这条是硬的，理由见下。

### 为什么不是取最高分

2026-07-30 实测 21 段：配音原文在 **11 段**上分数最高，查询字段只在 8 段最高。
按分数取最优会很诱人，但它会把已经对的段落弄坏：

| 段 | 查询字段 | 配音原文 | 实情 |
|---|---|---|---|
| 17 | **0.855**（逐字引用「别跟我道歉 我讨厌你的做法」） | 0.559 | 查询是精确引语，取配音会换成一个不相干的镜头 |
| 4 | 0.506 | **0.750**（配音全文就是「雪之下雪乃。」） | 高分来自「有人说出她名字」——**那个镜头拍的是说话人，不是她** |

**分数高不等于画面对。** 分数量的是「旁白文本与字幕文本的语义相似度」，
而我们要的是「这个镜头是否呈现旁白在讲的那一刻」。两者只在查询写成台词语义时才重合。
段 4 那种高分正是上期封面挑不出主角的同一个偏置：检索命中的是**谁说了这句话**，
镜头就在说话人身上。

> 同一条教训第三次出现：**一个量能用来卡门槛，不代表它能用来排序。**
> 前两次是拉普拉斯方差给封面排序、以及回读 CER 判轴对不对。

### 加大 TOPK 救不了落空

`TOPK` 只决定取前几名，而结果是按分数降序的——**top-1 低于门槛时，第 25 名只会更低。**
数学上不可能靠加大 K 把不及格变及格。

它只在另一种情形有用：及格的命中存在，但全被别的规则挡掉了
（该集没登记、片段超出片尾、画面已被别的段占用）。2026-07-30 实测本期 21 段，
**这种情形一次都没发生**：除段 8 外每段都有及格命中，且一条都没被 `candidate` 挡掉。
所以 `TOPK = 24` 暂不动，等真出现「及格命中全不可用」再说。

### 三级都不及格

写 `"status": "no_match"`，**渲染阶段硬失败，由人在第 05 步解决**。

不做自动降级（空镜 / OP 片段 / 纯文字排版）。原先文档承诺过降级，代码从未实现，
两者分叉了很久——**2026-07-30 决定按代码的实际行为改文档，而不是补一个凑合的降级**。
理由是「留白远优于画文不符」这条只有人能判：段落讲什么、留白合不合适、
换哪段素材顶上，机器都判不了。硬失败把这个判断交回给人，比自动塞一段空镜诚实。

**真正落空的绝大多数原因是查询写错了，不是素材不够。** 2026-07-30 踩的：
片尾套话的查询写成「角色转身走出教室」——那是画面描述，而索引建在字幕上，
字幕里没有画面信息。改成台词式的「时间差不多了 我该走了」，分数 0.446 → 0.747。
所以遇到 `no_match` 先回去看查询是不是在向字幕索引要画面构图。

## 截取守卫（渲染阶段强制，缺一不可）

请求时长超过源片剩余长度时，**ffmpeg 静默截断且不报错**。检索命中集尾时必然踩到，
一旦踩到整条音画全线错位而无任何提示。已在 2026-07-28 探针中实测复现。

1. **切之前**校验 `seek + duration <= 源时长`，不足立即失败
2. **切之后**复核实际时长与请求值之差 <= 1 帧

**「1 帧」要按源片实际帧率算，不许写死。** 原文这里写的是 `0.034s @30fps`，
但 2026-07-29 实测六集片源全是 **23.976 fps（24000/1001）**，一帧 0.0417s——
按 0.034 卡会把合格的切片judge成不合格。番剧片源常见 23.976 / 24 / 29.97 三种，
容差从 `ffprobe` 读到的 `r_frame_rate` 现算。

## 质检门禁

渲染后自动跑，机器可判定，不达标直接打回：

- 响度：EBU R128，目标 -16 LUFS（`ffmpeg -af loudnorm`）
- 削波：无 true peak > -1 dBTP
- 音画时长对齐：画面轨总时长与音频轨误差 < 0.5s
- 字幕不超框、无空段
- 成片无黑帧 / 无静音段 > 2s

主观项（节奏、观感）不进门禁——机器判不了，而且它们不该在成片之后才处理：
节奏问题的根在稿子和排片上，回到第 02 / 05 步改，不在成片上补救。

### 门禁的失败必须指向真实缺陷（2026-07-30 增补）

一道门禁只有两种坏法，两种都见过了：**该拦的没拦**，和**拦了不该拦的**。
后者更隐蔽，因为它看起来像门禁在尽责。

四次实例，成因是同一个——**判据测的不是它自称在测的东西**：

| 门禁 | 报的错 | 真实情况 | 判据错在哪 |
|---|---|---|---|
| 字幕对轴（`ingest verify`） | 「轴对不上，不许进索引」 | 轴完全正确 | Whisper 进复读循环，CER 1297%。把「ASR 坏了」当成「轴错了」 |
| 回读质检（`tts`） | 「重试 3 次仍不达标」，整期退出 | 音频完全正确 | 「她帮你，但她不让你欠她」被听成「他…」。**他/她/它 同音，ASR 不产出能区分它们的信息** |
| 稿件机检（`check_script`） | 「有论文连接词：最后」 | 「最后那一集」是时间指代 | 关键词匹配不看位置，也不管这个词身兼两种用法 |
| 回读质检（`tts`） | **PASS** | 「谁更该“赢”」念成「谁更该**非赢匪**」 | 参考文本被 `normalize` 剥掉了引号，回读多出的两个字只算 2 处插入，CER 7% 远在门槛下 |

修法都是**回到成因**，不是调阈值：

- 复读幻觉 → 输出长度超字幕 2 倍即判该采样窗不可用，**跳过而非记为反证**
- 同音字 → 在 `normalize` 里折叠 他/她/它/牠、妳。**只折读音完全一致的**：
  的/得/地 看着同档，但「得」有 dé、「地」有 dì，折了会掩盖真的念错
- 论文连接词 → 要求出现在句首或句读之后；「最后」额外要求后跟逗号
- 引号被念出来 → 合成前用 `tts.speakable` 剥掉纯书面符号（引号、括号、书名号），
  **逗号句号破折号一律保留**（它们控制停顿，剥了语速会乱）；
  同时给稿件机检加一条「无引号」，与原有的「无括号」同源

**判断一道门禁该不该拦，问它拿到的信息里有没有能证伪的东西。** 没有就该跳过该样本，
不该定罪——把「我不知道」记成「不合格」，是最容易让人干脆关掉门禁的做法。

### 归一化会把缺陷一起归一掉（2026-07-30 增补）

第 4 例值得单独记，因为它的坏法和前三例相反：**门禁判了 PASS，而产物是坏的。**

成因是同一个 `normalize` 被两个目的复用：

1. 比对时抹掉双方都不承载的差异（标点、同音字）——**这是对的**
2. 而引号恰恰是「模型多念了东西」的**唯一证据**，抹掉之后证据没了

**凡是「先归一化再比对」的检查，都要问一句：被归一化掉的那部分，会不会正是缺陷所在？**
标点在这里既是噪声（ASR 不还原标点）又是信号（TTS 不该念出标点），
同一个符号在两个方向上意义不同，一刀切必然漏一边。

这一例是**靠人听出来的**，没有任何机器检查报警。所以：
**成片交人之前，至少听一遍开头和最长的那一段。** 十一项质检全绿不代表能听。

## 版权与发布纪律

- 二创混剪 + 原创口播属灰区，加口播是加分项；**纯搬运不做**
- **BGM 用该番自己的音乐**——OST、OP、ED、插曲、角色歌都可以，见下方「BGM 约定」
- **上传手动做**，不自动化。省下的几分钟不值得拿账号冒险
- 片源不进 git、不外传

原先这里写的是「用纯音乐库、压低音量、不放完整曲目」，**2026-07-29 作废**。
作废依据是实测：已发布的全部作品都使用番剧 OP/ED/插曲作 BGM，未出现下架、静音替换
或可归因于音频的限流（样本量见 `CLAUDE.local.md`）。这个量级上指纹检测不构成实际约束。

**注意这是小样本结论，不是通则。** 换平台、换账号规模、或平台改了检测策略，
这条就可能失效——重新验，别照搬。

规则要么反映实际做法，要么删掉。留着不执行的条款会让整份文档失去权威。

## BGM 约定

**一部番配一套固定曲目，不零碎化。**

零碎化指两件事，都要避免：

1. **每期重新翻曲库挑歌。** 春物光 CD 就有 103 个 flac，每期从头挑一遍，
   既吃时间（顶到 ≤10 分钟的人类预算）又让每期听感彼此无关。
2. **一期之内切好几首。** 2–4 分钟的片子换三次 BGM，观众只会觉得吵。默认一期一首，
   最多两首（正文一首、结尾一首）。

**选曲是 Phase 0 动作，不是每期动作。** 一部番入库时定下 3–5 首的固定曲目表写进笔记，
之后每期只在这几首里挑，不再回曲库翻。这个数字是起点，用下来不够再加。

理由不只是省时间：**同一套 BGM 反复出现会形成听觉识别度**——观众听见前奏就知道这是你的春物系列。
每期换歌等于把这个免费的品牌资产扔掉。

曲目表有两份，各有各的读者，改一份就要同步另一份：

| 位置 | 给谁 | 内容 |
|---|---|---|
| `config/bgm.json` | 机器 | 按番分组，相对路径 + 实测指标。`pipeline/bgm.py` 与 `render.py` 只认它 |
| `data/library/notes/<番>.md` | 人 | 选了哪几首、为什么，和分集笔记同一个文件 |

放笔记里的理由不变：曲目表与分集笔记是同一件事，**这部番的可复用资产**。
但机器不该去解析 markdown 表格，所以另存一份 json。

### 每首必须记实测响度（2026-07-29 增补）

**曲目表里没有 `lufs` 的曲子不许用**，`pipeline/bgm.py` 会直接报错退出。

原因是实测出来的：劇伴 -14.4…-18.1 LUFS，OP/ED 单曲伴奏 -7.1…-13.2 LUFS，
**极差 11 dB**——单曲是按打榜标准压过的，原声碟不是。

初版渲染用的是固定系数 `volume=0.35`，那个数是对着 繋ぎとめた世界（-16.9）调出来的，
换一首就失准。拿它配 芽ぐみの雨（-7.1），BGM 会凭空大 9.8 dB 直接压过口播，
**而且不报错**——只会某一期突然特别吵，等发出去才发现。

所以音量的控制量是「归一到目标响度」，不是「乘一个系数」。目标取当初那组参数的等效值
`-26.0 LUFS`，每首按 `目标 − 实测` 折算增益。量法：

```bash
python -m pipeline.bgm measure data/library/bgm/<番>/*.flac
```

### 扫曲库必须解 cue，不能只数文件（2026-07-29 增补）

**原声碟是拆好轨的（一首一个 flac），OP/ED 单曲碟是整轨镜像 + cue。**
一张单曲碟只有一个 `GNCA-0380.flac`，四首歌全在里面。

2026-07-29 定春物曲目表时按 flac 文件数扫曲库，六张单曲碟各自只显示为「一个文件」，
**整档 OP/ED 被漏掉**，而这一档恰恰是最强的——观众听见 OP 前奏就有情绪，劇伴给不了。
更打脸的是，每张单曲碟都带 instrumental 轨，「OP/ED 有人声会跟口播打架」这个不选它们的
理由从一开始就不成立。

```bash
python -m pipeline.bgm scan <CD 目录>                    # 解 cue，列出所有 instrumental 轨
python -m pipeline.bgm extract <CD 目录> --anime <番>     # 切成独立 flac 落 data/library/bgm/<番>/
```

## 环境

Python 3.12 / 3.13 / 3.14 都可以，用 uv 建虚拟环境，不要依赖系统解释器。

> **原先这里写的是「部分 ML 包（torch / WhisperX）可能尚无对应 wheel」，那句话是错的**，
> 而且 WhisperX 早就不是依赖了（换成 mlx-whisper）。2026-07-30 实测三个版本各跑 165 条
> 测试全过。真正的约束是 `numpy<2.5`——依赖链 `mlx-whisper → numba → llvmlite`，
> numba 0.66 要求 numpy<2.5，numpy 一上 2.5 就会把 numba 回退到 0.53.1，
> 那个版本的 llvmlite 只支持 Python <3.10，转去源码构建并失败。
>
> **教训是「可能」这个词。** 那句话带着「可能」，说明写的时候没验，而它一旦落进
> `requires-python` 就变成了硬约束，还顶掉了对真正约束的排查——上界写错了地方，
> 结果是照 README 在 3.12 上全新装也装不上，而已有的 venv 一切正常，所以没人发现。
> **凡是要写进配置的判断，都不许带「可能」。** 验不了就先不写。

合成阶段全程 ffmpeg，无 NLE 依赖。ffmpeg 8.0.1 已确认带 libass / libfreetype / libfontconfig，
端到端探针已通过（见 ADR-0001）。中文字幕字体用 `/System/Library/Fonts/Hiragino Sans GB.ttc`。

**不要引入 MoviePy**（比直接调 ffmpeg 慢一个量级且不增加能力）。
表现力撞到天花板前**不要引入 Remotion**。需要人工精修的单期走 MLT XML（Shotcut）逃生舱，
不为此在主循环保留任何自动化链路。

## 验证

```bash
# 纯函数测试。3 秒，不碰片源 / 模型 / 外置盘，改完代码先跑这个
pytest

# 环境自检：SSD 挂载 + 工具链
./pipeline/preflight.sh

# 稿件机检
python -m pipeline.check_script data/episodes/<本期>/02-script.md

# 配音（含逐段 Whisper 回读质检，不过自动重试，三次不过整体退出）
python -m pipeline.tts data/episodes/<本期>

# 单句试音，用来比音色 / 比引擎，不进每期循环
python -m pipeline.tts probe "随便一句话" --ref data/voice/reference/audio_03.wav
```

**任何会下载模型的模块，第一个 import 必须是 `pipeline.paths`**——它负责把 `HF_HOME`
钉到 SSD。漏了就会往内置盘写几个 G，而且不会有任何报错。

### 写测试的两条纪律（2026-07-30 增补）

`tests/` 只测纯函数，**不许为了提覆盖率去 mock 掉 ffmpeg 或模型**。涉及媒体的正确性
交给截取守卫和质检门禁在真跑时兜——它们检查的是真实产物，mock 检查的是 mock。

**一、期望值先在实现上跑一遍，再写进断言。**

写 `clips.size` 的测试时有一条我想错了：以为「起点 1395、片长上限 1400、要 20 秒」
必然判 short，实际它会往前拉起切点到 1380，返回 ok——代码是对的，我的期望是错的。
照着「我以为它应该怎样」写断言，等于把错误期望固化成测试，下次改对了反而报红。

这不等于「跑出什么就断言什么」。跑出来的值先要看懂**为什么是这个值**，说不通就是找到 bug 了。

**二、写完做变异检验：把被测行为故意改坏，看测试红不红。**

新写的四个文件都过了这一关，例如把 `check_script` 摘片尾那两行改成 `pass`
→ 2 条红；把 `paths.conf` 的返回改成 `return cur or default` → 1 条红。

理由和这个项目所有其他规矩同源：**一条永远绿的断言和没有断言，效果完全一样，
但它会让人以为这块被覆盖了。**这又是一种静默失败。
