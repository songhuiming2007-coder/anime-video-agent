# CLAUDE.md — anime-video-agent

本文件是**常驻规则**：每次会话载入，只放必须每时每刻生效的约定。判据的完整证据、每步怎么做、开发顺序在各文档，用到时去读——长文件没人读得完，会失权威（本节「判据要么被执行，要么被删除」同样适用于本文件）。

只对这台机器 / 这个账号成立的事实（盘符、账号播放数据、优先级）在 `CLAUDE.local.md`，**不进 git**。

## 文档地图

| 文件 | 记什么 | 什么时候读 |
|---|---|---|
| **本文件** | 常驻规则：定位、硬约束、判据、红线、命令 | 每次会话 |
| `docs/INDEX.md` | **文档总入口**：1 个主题 → 1 个主文件 + 最多 1 个 ADR | 不确定查哪份文档时 |
| `docs/STANDARD.md` | 规范定义：每条标准的「为什么」与违反程序 | 做取舍 / 写判据 / 改标准时 |
| `docs/WORKFLOW.md` | 九步与 Phase 0 的操作细节 | 跑某一期 / 跑 Phase 0 时 |
| `docs/ROADMAP.md` | 开发顺序与闸门 | 决定下一步做什么时 |
| `docs/issues/README.md` | 未解决问题单表 | 找当前卡点 / 决策点时 |
| `docs/plans/README.md` | 可执行施工图索引 | 要动工改实现时 |
| `docs/adr/` | 难以回头的决策与证据、推翻条件 | 动某块架构前先读对应 ADR |
| `skills/write-script/` | 写稿指令：五种题材骨架与判据 | 第 02 步写稿时 |
| `README.md` | 全流程介绍与前置要求 | 给不了解项目的人 |

## 一、定位

用 agent 流水线批量生产动漫二创视频。**实验项目，优先级低于主线。**

**成功标准是流水线本身**，不是内容表现：交付物 = 一套可复用的 agent 工程系统；内容数据（播放、涨粉）= 验证信号。任何取舍都问：这件事让**流水线**更好，还是只让**这一期视频**更好？后者一律降优先级。

**可复用性总纲：这个项目不是为一部动漫写的，是为 1 亿部写的。** 机制进代码、内容进配置；判断是机制还是内容，问「换一部番它变不变」——变就是内容，不许写死。换番只动 config 与 Phase 0 资产，不改 `pipeline/`；阈值逐番标定，不许抄默认值；索引按番隔离，跨番命中是静默失败。**任何「换一部番就要改代码」的地方都是缺陷，不是惯例。** 完整七条见 `docs/STANDARD.md` 第二节。

**不做的事**（理由见 `docs/STANDARD.md` 第一节）：纯搬运、自动上传、无限打磨、政治议题进自动管道、引入 NLE/视频框架（MoviePy、Remotion）/数据库/编排/web、mock ffmpeg 或模型、检索落空自动降级、把审美判断包装成机器判据、调阈值绕过门禁。

## 二、硬约束（违反即视为系统缺陷）

- **每期人类投入 ≤ 10 分钟**，人类在 02.5（人审改稿）、03.5（配音顺听）、05（审时间码）与 09（发布）出现——2026-08-05 补上 02.5，2026-08-20 补上 03.5（防多语种自回归 TTS 发飘）。超预算 = 流水线有 bug：**改流水线或砍掉环节，不是加时间**。**连续 3 期超预算 → 停止产出，回头修流水线**（2026-08-09 删掉 meta.md 产物，耗时改为会话内我主动记录、会话结束复盘）。加了 02.5 之后总量已经理论超出 10 分钟上限，跑几期实测再看这条线要不要动。
- **禁止无限打磨。** 画质、转场、文案润色边际收益极低；时间只花在更自动、更可靠上。
- 建设期止损线（3 周 / 每周 ≤ 6 小时）**已满足解除**，现在唯一活着的止损线是上面那条 10 分钟。

## 三、判据（本项目的灵魂，全部硬标准）

完整证据与踩坑实例见 `docs/STANDARD.md` 第三、四节。核心判据：

1. **每加一道检查，先问它测的是不是真实产物。** 测的不是真实产物，通过就毫无意义（字幕项曾算「原文长度÷每行容量」，而 libass 对无空格的中文根本不折行）。
2. **一个量能用来卡门槛，不代表它能用来排序。** 同一教训踩过四次：拉普拉斯方差给封面打分、回读 CER 判轴、查询阶梯取最高分、跨通道分数融合。
3. **门禁失败回到成因，不调阈值。** 门禁只有两种坏法：该拦的没拦、拦了不该拦的。调阈值是拿一个真问题换一堆假问题。
4. **门禁拿不到能证伪的信息时，跳过样本，不定罪。** 把「我不知道」记成「不合格」，是让人干脆关掉门禁的最快方式。
5. **先归一化再比对的检查，问被归一化掉的部分是不是缺陷所在。** 标点既是噪声（ASR 不还原）又是信号（TTS 不该念出）。
6. **判据要么被执行，要么被删除。** 留着不执行的条款会让整份文档失去权威。
7. **凡要写进配置 / 代码的判断，不许带「可能」。** 验不了就先不写（`numpy<2.5` 那次上界写错地方的教训）。
8. **素材边界的判据是「集数对得上」，不是「检索得到东西」。** 后者永远为真。
9. **跳过不是通过。** 缺前置文件跑不了的检查必须显式列出，退出码也算不合格。
10. **分数只在它自己的通道内排序，绝不跨通道比较。** 台词的 0.7 和画面的 0.7 长得像纯属巧合。
    **唯一例外：presence 在场分**（ADR-0004）——它只在台词通道内、同一段落、
    台词分差 ≤ `PRESENCE_BAND` 的同一档候选间做 tie-break，绝不跨段落、绝不跨通道、
    绝无复合分；这个例外本身就是判据的落实：排序轴仍是台词分，presence 只细化它。
11. **对抗必须来自零上下文的 agent，同 agent 换立场无效。** 看过笔记的 agent 已被笔记的
    因果框架带走，找得出「顺不顺」、找不出「笔记与事实的分歧」。对抗审查报告必须含
    **随机抽样通过清单**——只报错不报过的报告是选择偏差，无法校准整体可信度
    （2026-08-13 罪恶王冠实测：45 条抽样通过 + 14 条问题）。对抗审查后必须有
    **终审裁决**：逐条采纳/驳回（驳回写理由）、修正回写笔记、裁决与报告存档——
    没有终审的审计等于白审。
12. **引文时间码必须行级对准。** 指到台词所在行，不是场景起点。实测 6 处时间码偏差
    全部是「场景起点 vs 台词行」的差（3-21 秒），写稿 agent 按错码检索会命中相邻的
    另一句台词。
13. **切分标定：过切无害、漏切有害，拿不准往低取。** 依据 ADR-0003 与三番标定实录
    （证据在 `config/project.json` 的 `_scene_threshold_note`）。

## 四、失败处理

- **诚实失败优于凑合交付。** 拿不到关键素材、配音反复不自然、检索无匹配——立即停下报告，不许降低标准硬交。这是本项目最重要的一条。
- **不许硬塞不匹配的画面，留白远优于画文不符。**
- **检索落空走查询阶梯**：`查询` → `备选` → `配音` 原文，逐级重试，门槛 `0.45` 不动。**只救不比**——上一级够格就到此为止，绝不因下一级分数更高而换。加大 TOPK 救不了落空（结果按分数降序，top-1 不及格时第 25 名只会更低）。
- **三级都不及格 → `"status": "no_match"`，渲染硬失败交人处理，不自动降级。** 落空绝大多数是查询写错了：写台词语义，别向字幕索引要画面构图。
- **成片交人前至少听一遍开头和最长的那段。** 十一项质检全绿不代表能听（归一化会抹掉证据）。

## 五、工程约定

### 代码

- **每阶段产物落盘，产物即状态**；人机交接走文件，不走对话上下文。任何一步失败可从中间接上重跑。
- **每个常量后面写「为什么是这个数」，每个 workaround 后面写「不这么做会怎样」。** 拍脑袋的数不许进代码。
- 路径一律相对仓库根（`./data/...`），不硬编码卷路径（含空格、卷名会变）。
- **任何会下载模型的模块，`from . import paths` 必须排在所有第三方 import 之前**——它把 `HF_HOME` 钉到 SSD，顺序错了会静默往内置盘写几个 G。
- 依赖装项目内（uv 虚拟环境），不污染全局。新增能力开新文件。

### 存储

- 仓库只有纯文本（代码、skills、配置、文档），进 git；大件（片源/字幕/索引/模型/每期产物）在 `data/`，**不进 git**。`data/` 可以是外置盘符号链接或实体目录。
- **脚本绝不自动创建 `data/`，不可达立刻报错退出。** 符号链接指向未挂载的卷时，mkdir 会把几十 G 静默写进系统盘、且盘插上后看不见。建骨架是显式动作：`./pipeline/preflight.sh --init <目标目录>`。
- HF 缓存：本项目在 `data/models/hub/`（由 paths.py 钉住）；机器上其他工具走外置盘 `hf-cache/`。**盘没插时 HF 工具全报错——这是有意的**，宁当场失败不静默灌内置盘。
- 目录：`pipeline/`(各阶段脚本，每个可独立运行) `skills/`(写稿 skill) `config/`(project/voice/bgm/characters/scenes，机制与内容的分界见各文件 `_note`) `tests/`(纯函数测试) `docs/`。

### 素材硬规则

- **字幕的轴必须照手上这个视频文件打，只许三种来源**：① ffmpeg 提取内封 ② 对该文件 ASR ③ 同发布、文件名逐一对应、**且过 `ingest verify` 对轴校验**的外挂。**永不用第三方字幕配第三方视频**——跨版本差异是插入与删除，offset 修不好；失败是静默的，视频照常渲染只是画面对不上话。
- **片源完整性判据 = ffmpeg 全片解复用无报错**（`ingest intact`）。块比例只当**前置快筛**：「没下完」可信、「下完了」不可信；判据必须自包含，不查下载器 API。
- **Phase 0 一次做完全集，做不完不许出片。** 判据是六条数字相等（片源 / 字幕索引 / 笔记 / 镜头表 / 角色在场 / 画面语义），`python -m pipeline.vindex status --anime <番>` 一查全查。整季入库三步合一，不许拆开只跑一两步：`python -m pipeline.ingest phase0 <该季 mkv> --anime <番> --season <N>`。
- **一部番榨干再换**：选题池枯竭前不开新番；番剧笔记写厚 + **零上下文对抗审查** + **终审裁决回写**（全部期共用的资产，全程 agent 劳动、人类零耗时，细节在 `docs/WORKFLOW.md`「番剧笔记」节）；换番要显式决定并在会话里跟 agent 记一句为什么。

## 六、每期九步

产物即状态，看目录里有哪些文件就知道到哪一步。顺序**不可交换**：必须先配音拿到每段真实时长，再排画面轨——字数估算攒出几秒渐进错位，越到后面越歪，**全程不报错**。人类在 02.5（写完稿子先审改）/ 03.5（配音顺听）/ 05 / 09 出现。每步命令与判据见 `docs/WORKFLOW.md`。

| 步 | 01 选题 | 02 写稿 | 03 配音 | 04 排片 | 05 审时间码(人) | 06 渲染 | 07 质检 | 08 封面标题 | 09 发布(人) |
|---|---|---|---|---|---|---|---|---|---|
| 产物 | `01-topic.md` | `02-script.md` | `03-audio/` | `04-clips.json` | `04-clips.approved.json` | `05-final.mp4` | `06-check.log` | `07-cover/`+`07-titles.md` | — |

- **01 必须带 `类型`**（人物志/剧情回顾/杂谈/盘点/共鸣）：骨架由题材决定，写稿只校验、不更换。`模式`（驳论/立论/吐槽）与 `张力` 只对杂谈必需——张力是整条流水线里唯一真正的编辑判断，必须在这一步定死并经人批准，不能留到写稿阶段由 agent 自行发明。**人物志/剧情回顾/共鸣不设张力**：它的观点就是人物/故事本身，由素材决定，选题阶段硬造一个论点再回头挑经历，是哗众取宠、没有同理心（2026-08-09 人物志踩过）。**人物志以称赞为主，不讨论创作争议**（幕后制作、炎上、换剧本之类）——写稿与素材挑选都只取作品内容本身，争议仅存档不推荐（2026-08-10 楪祈素材踩过：把第五集输血/绿帽之争标成了可用素材）。
- **02 的 `查询` 是机器接口**：被原样送进索引检索，写成台词语义；`集` 字段同样（ADR-0004）——集号被原样送进索引做集内掩码，从引用核对里查，不是拍脑袋。写完自检两遍且顺序不能反——先保真（台词原文/说话人/集号/数字）再去味。
- **03 按句合成不按段**（语速与文本长度正相关），每段时长以 `ffprobe` 复核写进 `manifest.json`，不采信模型自报。
- **05 是唯一人工关卡**，`--approve` 必须是显式动作（曾因自动写 approved 而形同虚设，两文件字节相同就渲了）。
- **04 视觉通道二选一，路由不是融合**：`人物: 某某` 走台词检索+角色在场过滤；`场景: 描述` 走画面语义检索。一段只走一个通道，两个都写当场报错；分数只在自己通道内排，分派时台词段先挑。**`场景` 当前不可用**——第 2 层探针没过（门槛立不住，ADR-0003 待实测 #4），写了会当场报错，这是终态。
- **08 分工三层**：机器筛（黑场/过曝/糊帧/去重，逐条有判据）→ agent 看联系表挑（认人、占不占主位）→ 人点一张（审美）。**9 张候选不排名**——清晰度是准入门槛不是排序依据，打分版本两版都把正确答案压掉了。
- **检测不对称：封面硬过滤，排片软过滤。** 封面按「含主角」过滤，为空则失败；排片角色过滤为空则**退回不过滤的结果**。理由：「检测到 X」可信，「没检测到 X」不代表不在场（侧脸/背影/遮挡会漏）。

## 七、截取守卫与质检门禁

**截取守卫（渲染强制，缺一不可）**：切前校验 `seek + duration <= 源时长`；切后复核实际时长与请求之差 ≤ 1 帧。ffmpeg 在请求超出片尾时**静默截断且不报错**，检索命中集尾时必然踩到，一踩整条音画错位。「1 帧」按 `ffprobe` 的 `r_frame_rate` 现算，不许写死（23.976fps 一帧 0.0417s）。

**段级不变量：每段 Σclip.dur == manifest 段时长（±0.05s），approve 与 render 双重拦截；05 人审改画面只改 start/source，改完跑 `python -m pipeline.clips <本期> --refit` 重排版。**

**质检门禁（渲染后自动，不达标打回，不进人工环节）**：时长落在目标带（默认 2–4 分钟，
可被该期 `01-topic.md` 的 `时长目标` 字段覆盖，见「九、内容参数」时长行）；音画误差 < 0.5s；响度 −16±1.5 LUFS、true peak ≤ −1 dBTP；无 >0.5s 黑帧、>2s 静音；字幕无空段、不超宽、每卡一行、不超高。**主观项（节奏、观感）不进门禁**——根在稿子和排片，回 02/05 改，不在成片上补救。

**配音回读质检**：每段生成后 Whisper 回读，不过换种子降温重生成，三次仍不过整体退出，不留坏音频。比对在**带声调拼音**上做（`tts.syllables()`）——按字形比，量到的是「ASR 认不认识人名」，不是「TTS 念没念对」；同音字（他/她/它）折叠只折读音完全一致的。含英日歌名段 CER 可能虚高（回读听岔假名），歌名区段做豁免，不放松整段阈值（ADR-0006）。**ASR 盲区豁免（2026-08-16 改）**：三个不同种子生成的音频 Whisper 全部严重失真（CER 远超门槛）而时长达标时，判为 ASR 对音色的盲区而非 TTS 念错——跳过样本不定罪（S4），保留音频标 `qc_skip`，成片交人前必须人耳确认。豁免判据就是「3 次全不达标 + 时长达标」，**不再要求回读文本两两相似**（2026-08-16 段落 5.1 实测：失真模式在假名乱码与近音中文间摇摆，内容互不相似而音频念对；旧判据会卡死整期）。跑飞由时长闸拦（漏读/重复改变时长）+ 人耳确认兜底。

## 八、清理约定

- **时机：人拍板最终版之后**（选定成片、封面、标题）。在那之前所有中间产物不动。清理前每次主动问，不许自己删；脚本只报体积和路径，**不许替人判「什么时候算定稿」**。
- **判据是「重生成要多久」，不是「占多大」**：几十秒能重来的一律可清；不可重生成的（片源、参考干声、番剧笔记、BGM 分轨）一律不清，哪怕占几百 M。

| 类别 | 例子 | 定稿后 |
|---|---|---|
| 编号产物 | `01-topic.md` … `07-cover/`+`07-titles.md` | **永久留**，产物即状态 |
| 成片与配音 | `05-final.mp4`、`03-audio/` | 留（改稿重渲要用） |
| 中间缓存 / 交接副本 / 会话临时 | `07-cover/raw/`、`04-thumbs/`、试听 wav、`02-script.draft.md` | 问过就删，可重生成 |

## 九、内容参数

来自自己账号的实测复盘，不是拍脑袋。**具体播放数字在 `CLAUDE.local.md`**（账号数据，不进 git 且会过时），这里只留规则与方向性依据。改动任何一条前先看新数据是否推翻它；推翻了**先改规则、再改实践**。

| 参数 | 标准 | 依据 |
|---|---|---|
| 平台 | 抖音优先，B 站同步 | 同内容抖音播放高一个量级 |
| 画幅 | **16:9 横屏，已决策** | 番剧构图按 16:9 设计，裁竖屏破坏画面 |
| 时长 | 2–4 分钟（默认，可按期覆盖） | 两平台一致：时长与播放量负相关——这条数据来自短打杂谈类内容，不假定对所有体裁都成立。选题天然需要更多篇幅时（人物志/剧情回顾/共鸣等），在该期 `01-topic.md` 写 `时长目标: X-Y分钟`，`check_script.py`/`qc.py` 会跟着改判据；不写就还是这两平台数据撑着的 2–4 分钟。**这是一次性的选题决定，不是把默认值改宽**，理由在会话里跟 agent 说清楚即可 |
| 选题 | 情感共鸣密度 × 番剧认知度，不是热度榜 | 认知度最高的热血番反而垫底 |
| 垂直度 | 硬约束，技术/体育类一律不发 | 播放近零，稀释账号标签 |
| 素材配比 | 动画 ≥ 80%，漫画 ≤ 20% 补位 | 受众预期动态画面 |
| 标题 | 金句式，不论文式 | 判据：能否脱离视频单独发出去 |
| 政治 | 不进自动管道 | 实测被退回/限流，agent 不掌握审核边界 |

**文案由 Agent 全权生成**是知情的取舍（人工写稿约 30 分钟/期，会吃掉学习时间），但必须是可替换模块：`config/script-mode = auto | from-core`，切换只改配置、不改流水线。

## 十、BGM 约定

- **选曲每期人耳现选**（2026-08-08 改，此前是「一部番一套固定曲目，Phase 0 定死」）：AI 的音乐审美、对经典歌曲的判断不如人，把选择权锁死在机器手里是本末倒置。Phase 0 只做扫库/分轨/量响度这些机械活，攒成一个可选的曲库；具体选哪首等这期配音出来、人听过之后再定，填进这期 `01-topic.md`（允许后补——选题阶段写不了，配音出来才听得出该配什么）。**支持任意首数拼接**（2026-08-10 改，此前「一期最多两首」）：`01-topic.md` 写 `BGM:` 列表，每行一首、`@N秒` 定起点（不写自动接上一首结尾）；老格式 `BGM正文`/`BGM中段`/`BGM结尾` + 切入点字段仍可用。每首放一遍、后一首接棒；后一首起点晚于前一首自然结尾 → 留停顿。最后一首铺到片尾。
- **每首必须记实测响度**：曲目表里没有 `lufs` 的曲子不许用（`bgm.py` 报错退出）。音量控制量是「归一到目标响度」（-26 LUFS），不是乘固定系数——劇伴与 OP/ED 伴奏极差 11 dB，固定系数换一首就失准且不报错。
- **扫曲库必须解 cue**：OP/ED 单曲碟是整轨镜像 + cue，按 flac 文件数扫会漏掉整档（而它恰恰最强）。
- 曲库两份，改一份同步另一份：`config/bgm.json`（给机器，攒的是候选池，不是定死的选择——`use` 字段现在只是没人现选时的退路）+ 番剧笔记（给人，和分集笔记同文件）。
- **该番所有下载到的音乐一律放 `bgm/<番>/`**（2026-08-09 定）：顶层是已测量、进 `bgm.json` 候选池的曲子（flat 文件，路径被直接引用，不能动）；`raw/` 子目录放完整原始下载——不管是独立下载的 OST 压缩包，还是随 BD 一起下的附赠 CD（Drama CD / 角色歌 / 单曲），只要是音乐就不留在 `raw/<番>/`（那里只放视频+字幕）。理由：音乐找起来独立于「按哪个 BD 组下的」，混在片源目录里下次想扩曲库还得先想起某季附了什么 CD。

## 十一、版权与发布

- 二创混剪 + 原创口播属灰区，加口播是加分项；**纯搬运不做**。
- **BGM 用该番自己的音乐**（OST / OP / ED / 插曲 / 角色歌）。例外须人拍板并在 `config/bgm.json`
  的 `from` 里写明来源与理由（2026-08-12 已立一例：罪恶王冠选用了 EGOIST 的
  `Last Song -Instrumental-`，出自 Bravely Second 单曲碟，非本番曲目——因该曲是本期主题的
  感情落点，人耳现选时拍板）——例外不留默认权，每次都要人点头。
- **上传手动做，不自动化**——省下的几分钟不值得拿账号冒险。
- 片源不进 git、不外传。
- **小样本结论，不是通则**：换平台、换账号规模、或平台改检测策略，这条就可能失效——重新验，别照搬。

## 十二、环境与验证

- Python 3.12 / 3.13 / 3.14，用 uv 建虚拟环境，不依赖系统解释器。`pyproject.toml` 的 **`numpy>=2.0,<2.5` 上界不能删**（依赖链 mlx-whisper → numba → llvmlite，numpy 2.5 会回退到装不上的组合；此坑**只咬新环境**，所以「我这儿能跑」不构成证据）。
- ffmpeg 必须带 libass（`preflight.sh` 查）。中文字幕字体 `Hiragino Sans GB`（`config/project.json` 里配置）。

```bash
pytest                                          # 纯函数测试，3 秒，改完代码先跑
./pipeline/preflight.sh                         # 环境自检：data 可达 + ffmpeg 带 libass
python -m pipeline.check_script <02-script.md>  # 稿件机检
python -m pipeline.vindex status --anime <番>   # 六条数字对不对得上
```

**写测试两条纪律**：① **期望值先在实现上跑一遍再写进断言**（不是「跑出什么断言什么」——先看懂为什么是这个值，说不通就是找到 bug 了）；② **写完做变异检验**——把被测行为故意改坏，看测试红不红。一条永远绿的断言和没有断言效果一样，只会让人以为这块被覆盖了（又一种静默失败）。
