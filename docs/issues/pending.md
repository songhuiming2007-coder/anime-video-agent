# 待决策（Pending）

有明确决策点、还没拍板的事。含触发式挂起（条件满足才需决策）。当前 13 条。

---

### [D1] ADR-0005 候选复核：四个子问题未定
- 状态：待决策
- 位置：docs/adr/0005:111-121
- 描述：候选复核（B1 的方向）动手前必须答：① 接在哪（clips.py 内部 vs 05 审图页辅助提示，
  影响「人是唯一关卡」原则）；② 复核不通过算什么状态（回退现有排序 vs 提升为 `no_match`
  交人）；③ 判据 10「分数只在自己通道内排序」是否适用于复核这个布尔/离散产物，以及要不要
  新写「复核意见 vs 检索分数冲突听谁的」规则（原文：「需要显式写清楚，不能悬着」）；
  ④ 成本与效果的探针方案。
- 线索：选项与顾虑已列全，无决定。
- 推进：B1 整体启动时一起立。

### [D2] 无台词画面的排片死角——写稿阶段强制确认机制未落实
- 状态：待决策
- 位置：docs/adr/0005:91-98
- 描述：被叙述那一刻没有台词（如翻花绳镜头）时，候选池里从一开始就不存在正确答案，任何
  排序机制都救不了。唯一解是写稿阶段（02）先用 `subindex.search` 确认有无台词、没有就显式
  标注人工指定 start/source。
- 线索：这是已确认的纪律问题，但还没落成写稿流程/SKILL 里的强制项。
- 推进：把「无台词段落显式标注人工指定」写进 skills/write-script 或 WORKFLOW 02 步。

### [D3] 机检字数下限逼注水——人物志是否放宽 min_chars 未定
- 状态：待决策
- 位置：docs/adr/0005:183-214（补记三、约束 4）
- 描述：机检字数下限逼 agent 注水（「一条一条数的是集的伤口」等机器味句子的直接成因，
  2101 字 < 2362 下界 FAIL）。「缩段不注水」是否对人物志放行需单独立判据。
- 线索：ADR-0005 补记已提议，check_script 的 min_chars 判据未动。
- 推进：下次写人物志前拍板：放宽判据 or 保留逼注水，选了就同步改 check_script。

### [D4] ADR-0003 第 3 层 VLM 启动条件挂起
- 状态：待决策（触发式挂起）
- 位置：docs/adr/0003:38-39, 95-141
- 描述：第 3 层（VLM 理解画面）「现在不做」但不是永久否决。启动条件：前两层跑过至少 3 期
  + 失败原因确为「需要理解画面」而非查询写错。第 2 层 2026-08-04 已否。
- 线索：附 5 条压时间做法（重排不建全量索引、压分辨率、短标签、3B 4bit、只打标死角），
  纯纸面未实测，第 1 条「一期约 100 帧几分钟」标了待实测（L140）。
- 推进：闸门 C 控制启动；真启动前先测那 5 条的时间假设。

### [D5] 第 2 层复活的未试两条路（需重新过探针）
- 状态：待决策（长期挂起）
- 位置：docs/adr/0003:431-433
- 描述：① 换动漫域的图文模型；② 把「是不是命中」交给第 3 层的 VLM 逐帧判断。
- 线索：两条路都要重新过探针，不许先上了再看效果。
- 推进：触发条件 = 将来需要画面语义通道时。

### [D6] 排片错配率完成判据「还没测」
- 状态：待决策
- 位置：docs/adr/0003:478；docs/ROADMAP.md:215
- 描述：ADR-0003 第 1 层完成判据两项：封面项已测（9/9 vs 基线 0/9），**排片错配率还没测**，
  要等一期真正用上 `人物` 字段的稿子。
- 线索：量化方法在 ADR-0005:29-35（04-clips.json 机器出 vs 04-clips.approved.json 人改的 diff，
  按查询写错/语义撞车/候选池无答案分桶），外置盘可用时回填。
- 推进：外置盘挂载时回填错配 diff，作为 B1/D1 的数据基础。

### [D7] 次回预告仍在索引里，未滤除
- 状态：待决策（低风险）
- 位置：docs/WORKFLOW.md:690-693；pipeline/subindex.py:66-96
- 描述：约占 2% 检索单元，有台词有轴、过得了所有判据，但画面是下一集内容。检索命中会配错画面。
- 线索：按 style 滤（同 OP/ED 歌词那条）大概能解决，还没做。
- 推进：给 subindex 的 style 过滤表补「次回预告」类 style。

### [D8] 纯中文字幕的集数进不了索引
- 状态：待决策
- 位置：docs/WORKFLOW.md:685-688；README.md:216
- 描述：`ingest verify` 要拿日文字幕对日语 ASR，只有纯中文轨（`.SC.ass`）的集没有可比的东西、
  过不了对轴校验。两条路（ASR 兜底转录 or 放弃那几集）都未走。
- 线索：无决定。
- 推进：素材边界完整性相关的决策，遇到纯中文字幕集时触发。

### [D9] ROADMAP 阶段 1/2 未做 + 共享 fixture 待建
- 状态：待决策
- 位置：docs/ROADMAP.md:48-113；CONTRIBUTING.md:38-48, 163-174
- 描述：阶段 1（Windows 跑通九步：ASR/TTS 解绑成可替换后端、修平台假设）未做；
  阶段 2（from-core API 接入，`config/script-mode` 的 seam 预留未实现）未做；
  跨平台共享 fixture（Blender 开源电影、Sintel）「现在还不存在」。
- 线索：阶段 1 依赖闸门 A（B3）；协作者侧无人维护时「整块标记不支持还是保 CI」也未定
  （ROADMAP:225-226）。
- 推进：闸门 A 触发后按 ROADMAP 顺序。

### [D10] ROADMAP 核心假设未验证 + GUI 边界未答
- 状态：待决策
- 位置：docs/ROADMAP.md:211-219, 176-195
- 描述：①「解说视频骨架通用」核心假设未验证（HELP.md:62）；②「两人平行开发不会分叉」
  靠 CI + fixture 兜，fixture 未建；③ GUI 的「给谁用」+ 审阅型/编辑型边界，ROADMAP 明写
  「两个问题不回答就不要动手」「现在就定死，不要等做的时候凭感觉」。
- 线索：GUI 属阶段 6 闸门 D，优先级最低；骨架通用性属核心假设。
- 推进：分别由闸门 A（泛化）/闸门 D（GUI）控制。

### [D11] 角色贴名未完成 + 「佑」显示名待核对
- 状态：待决策
- 位置：config/characters.json:23,51,65,97
- 描述：东京喰种「人贴名逐步补」未见完成标注（re 篇/√A/OVA 角色靠用户外貌描述辅助）；
  罪恶王冠「佑」译名只经中文维基交叉确认，本地 GB 字幕 grep 不到确凿具名台词。
- 线索：`_note_yuu` 建议「faces 贴名前先用 subindex search 核对角色实际出场台词怎么称呼他」。
- 推进：贴名做完（角色过滤覆盖才全），「佑」用 subindex 核对后再定显示名。

### [D12] BASELINE 语气词密度判据挂起（口语体样本仅 1 篇）
- 状态：待决策
- 位置：skills/write-script/BASELINE.md:80
- 描述：语气词密度（啊/吧/嘛）AI 0–0.33 vs 口语体 1.21 分得开，但「口语体样本只有一篇。
  样本量到之前不立规矩」。
- 线索：判据处于挂起，等补样本。
- 推进：补口语体样本后决定是否立门禁。

### [D13] 对抗性审查/写厚笔记新机制未经一期实战验证
- 状态：待决策
- 位置：docs/WORKFLOW.md:116-163；skills/write-script/SKILL.md:71
- 描述：2026-08-12 建立的机制（写厚四条硬门槛 + 全新 agent 独立考据对撞 + 未经证实不进正文
  依赖写厚），尚未经一期完整验证。
- 线索：按用户安排，下一部番剧开新库时实测。
- 推进：下一部番剧开库时跑完整验证，检验它是否真把 02.5 人工负担压下来。

### [D14] tts.py 的 CPM=280 硬编码与 config 校准值 315 分叉，注释声称同源是假的
- 状态：待决策
- 位置：pipeline/tts.py:37 vs pipeline/check_script.py:30 vs config/project.json:40
- 描述：`tts.py:37` 硬编码 `CPM = 280  # 与 check_script 同源`；`check_script.py` 读
  `paths.conf("script.cpm", 380)`，config 已校准为 315（2026-08-10）。两处相差 12.5%，
  `expected_duration()` 用它算 `want`、时长校验基于它。2026-08-10 改 config 时漏改了 tts.py。
- 线索：`test_tts.py:186` 断言的是 `t.CPM` 自洽，测不出这个分歧——注释与事实不符，属
  STANDARD「实现与文档分叉时改文档」要处理的静默错误。
- 推进：tts.py 改成读 `paths.conf("script.cpm", ...)`，或对齐 config 值；改完跑 pytest。

### [D16] 「人类只出现在两/三处」文档四处互相矛盾，02.5 加了但没同步
- 状态：待决策
- 位置：README.md:5 与 1.2 表；docs/WORKFLOW.md:5；docs/STANDARD.md:83；pipeline/review.py:6
- 描述：README「人只在两个地方介入」、WORKFLOW「只出现在三处：周选题、审时间码、发布」、
  STANDARD 硬约束表「只有 05 和 09」、review.py「只在 05 和 09」——四处与 CLAUDE.md 现行
  「02.5/05/09」全部对不上，WORKFLOW:5 还与自己 L316 的 02.5 一节自相矛盾。
- 线索：02.5 是 2026-08-05 加的，文档没跟上。这是文档陈旧，不是流程问题。
- 推进：统一改成 CLAUDE.md 的「02.5 人审改稿、05 审时间码、09 发布」三处，四处文件同步。

### [D17] 文档声称的测试数全部过时且互相矛盾
- 状态：待决策
- 位置：README.md:149,188,428（298 条）；CONTRIBUTING.md:42,156 与 .github/workflows/test.yml:8
  与 docs/ROADMAP.md:76 与 pyproject.toml:6（165 条）
- 描述：实际 `grep -c "def test_" tests/*.py` 求和 = **398 个 test 函数**。文档 298/165 与实际
  差 100+，且文档之间自己矛盾。
- 线索：无 skip/xfail/NotImplementedError/空测试体，全部 15 个测试文件无跳过项。
- 推进：以 pytest 实际输出为准统一回填五处数字。

### [D18] 集号/人物字段的「该写没写」无机器判据，错配静默发生
- 状态：待决策
- 位置：skills/write-script/SHOTLIST.md:70-73, 96, 105-109；pipeline/check_script.py:322-344
- 描述：`集:` 号只验「格式+存在」不验「是不是正确那一集」（写对靠引用核对自觉，写错要拖到
  05 人审的降级标记才暴露）；`人物:` 字段「该写没写」无判据——2026-07-30 踩过：配音原文
  「雪之下雪乃。」检索得分 0.750 很高，可镜头拍的是说话的人不是她，写上 `人物: 雪乃` 才能筛掉。
- 线索：这是 D6（错配率未测）和 B1（排片错配）的直接相关项——写稿侧自觉性缺口。
- 推进：等 D6 量化后决定：是否把「有台词引用的段落必须写 `集:`」提为机检强制。

### [D19] 说话人确认是全流程最不可靠环节（字幕 Name 字段基本不填）
- 状态：待决策
- 位置：skills/write-script/SKILL.md:73-74
- 描述：字幕索引的 `Name` 字段字幕组基本不填，检索命中后「说话人是谁」只能靠 agent 记忆，
  工具帮不上忙——SKILL 只能要求写「某某说」前抽帧目视。与 D2（无台词死角）是邻近但不同的
  结构性死角。
- 线索：无机器判据；靠人工抽帧兜底。
- 推进：与 D2 一起考虑：是否把说话人确认纳入对抗性审查的职责范围。
