# Plan: 基于云端 GPU 算力池的 v2 端到端多模态重构施工图

- **立项日期**: 2026-09-08
- **对应 Issues**: B1 (排片错配), B2 (人时超预算), D1 (复核子题), D2 (无台词排片死角), D4 (VLM启动), D5 (画面语义), D6 (错配率判据), D22 (多模态漏斗), D23 (配音顺听摩擦力), D25 (CJK音读泄漏治理)
- **关联 ADR**: ADR-0014 (计算与交互解耦), ADR-0003 (视觉索引), ADR-0005 (文本相似度≠语义理解), ADR-0006 (Qwen3-TTS)
- **执行时机**: **《EGOIST 编年史》第三期《终局葬礼》交付收官后正式启动（先收官，再重构）**

---

## 一、 为什么必须推翻妥协？（重构核心动因）

### 1. 荒谬的资源错配：高维 Token 在给低维算力擦屁股
截至 2026-09-08，本项目已累计消耗 **15 亿 Token、8,192 次 LLM 请求**。分析证明，超过 **80% 的 Token** 并非用于文案创作，而是消耗在**为 16GB Mac 本地算力缺陷擦屁股**上：
- 陪 1.7B 自回归小模型反复排查“全部→权部”、“世界→逝戒”等 CJK 同形字音读泄漏，反复修改错别字和重试随机种子；
- 因本地缺乏视觉大模型（VLM 失明），Agent 被迫将几万行的 `04-shots.json` 裸吞入 Context 人肉计算镜头起止时间，排查 `anchor_overlap` 冲突。

### 2. 题材跃迁的业务断层：杂谈 80 分 vs 纪录片 50 分
现有流水线在制作《春物》《天气之子》等动漫杂谈时可达 **75~85 分**，因为原番自带“台词即画面”的强绑定红利；但在制作《EGOIST 编年史》等深度音乐史与文学纪录片时，评分暴跌至 **50~60 分**：
- **台词脱节**：纪录片讲客观史实与命运张力（“合同只有六个月”、“女孩窒息在黑幕”），原片根本没有对应台词，字幕检索覆盖率为 0%；
- **异构素材**：从单番 MKV 扩张为跨番混剪、Live 现场、实体扫图微动、音画同源原声，机械时间轴碰撞频发；
- **意象通感**：纪录片需要“窒息落寞”、“万人狂热”的电影级意象蒙太奇，基于字符串字面相似度的算法根本无法理解。

### 3. 经济学账本彻底打破心智门槛
- AutoDL 租用 24GB 显存的 RTX 4090 仅需 **1.98 ~ 2.2 元 / 小时（约 0.036 元/分钟）**；
- 单期视频开机 10 分钟算力成本约 **0.25 元**，哪怕包机连续实验 1 小时也不过两块钱；
- 相比于购买万元物理工作站或消耗数以亿计的高价值 LLM Token，云端弹性 GPU 是绝对的压倒性选择。

---

## 二、 历史半路想法的一网打尽与全局收束

本方案将仓库过去挂起的所有临时想法、未竟探针与妥协补丁进行彻底收束清算：

```
┌────────────────────────────────────────────────────────────────────────┐
│                   历史分散的半路想法与工单收束清单                         │
├─────────────────────────┬──────────────────────────────────────────────┤
│ 历史工单 / 半路想法      │ v2 云端 GPU 架构终极解法                      │
├─────────────────────────┼──────────────────────────────────────────────┤
│ D2 无台词画面的排片死角  │ 4090 跑 Qwen2-VL 对全片镜头进行密集中文意象打 │
│ D4/D5 视觉索引 VLM 启动 │ 标，将“找台词”跃迁为“全片意象直接检索”，彻底  │
│ D22 多模态漏斗检索探针   │ 消灭无台词盲区，使排片真正具备导演级视听理解。│
│ B1/D1/D6 排片错配终局   │                                              │
├─────────────────────────┼──────────────────────────────────────────────┤
│ D25 CJK 同形字音读泄漏  │ 换上 7B 级语音大模型（Qwen2-Audio/CosyVoice） │
│ N12 readings 错别字地狱 │ + F5-TTS 流匹配（Flow Matching），由 7B 参数 │
│ D23 配音顺听人机摩擦力  │ 彻底解耦中日汉字；数学上根除跳字、复读和大叔音│
│                         │ ；跑 15 分钟 LoRA 微调将音色锁死在 98%+。     │
├─────────────────────────┼──────────────────────────────────────────────┤
│ 音乐纪录片原声割裂痛点  │ 引入 UVR5 / Demucs 神经网络分轨，将现场录像   │
│ (ADR-0013 音画同源延展) │ 瞬间分离出纯人声、伴奏、观众欢呼三路独立母带。│
├─────────────────────────┼──────────────────────────────────────────────┤
│ 老番/模糊现场画质痛点   │ 引入 Real-ESRGAN-Anime6B + RIFE 补帧，将老旧  │
│                         │ 720p 视频与扫图无损提升至 4K 60fps 院线画质。 │
└─────────────────────────┴──────────────────────────────────────────────┘
```

---

## 三、 v2 系统架构拓扑（Client-Server Headless GPU）

```
[ 本地轻量便携客户端 (MacBook) ]
   ├── 01 选题与考据策划 (Markdown)
   ├── 02 剧本写作与 02.8 对抗性审查 (LLM Agent, 严格控 Token)
   ├── 07 标题与发布简介定稿 (07-titles.md)
   ├── 08 封面渲染 (Pillow 参数板模 100% 照抄)
   └── 09 平台手工发布
            │
            │ Git 代码推送 / SSH 管道 / OSS 数据同步
            ▼
[ 云端 Headless GPU 算力池 (AutoDL RTX 4090 24GB) ]
   ├── [Phase 0 疯狂加料预处理]
   │    ├── Qwen2-VL-7B 逐镜头生成精准意象描述
   │    ├── BAAI/bge-m3 构建全功能跨语言意象向量库
   │    └── UVR5 抽取音乐分轨 + Real-ESRGAN 超分 4K
   ├── [03 配音工程]
   │    ├── 7B Flow Matching + LoRA 定制音色 (30秒出全片)
   │    └── SenseVoice-Large / Whisper-Large-v3 全量回读
   └── [04/05 排片与渲染]
        ├── 意象对齐排片生成 04-clips.approved.json
        └── ffmpeg NVENC 高速硬件混音渲染 05-final.mp4
```

---

## 四、 施工改动清单与技术选型

### 1. 配音模块重构 (`pipeline/tts.py`)
- **废弃**：`mlx-audio` 纯苹果后端绑定、`readings` 错别字字典、`segment_seeds` 探针；
- **引入**：PyTorch + CUDA 运行的 **CosyVoice 2.0 / F5-TTS**；
- **音色资产**：以 `seg6.wav` 结合 2 分钟干净原声，训练专属 LoRA 说话人权重；
- **注音**：挂载标准 `pypinyin` 规则库，输入端全部转为标准音素拼音嵌入。

### 2. 视觉多模态索引引擎 (`pipeline/vindex.py`)
- **废弃**：已证伪的 Chinese-CLIP 余弦搜索、死板的单帧二次元 tagger；
- **引入**：**`Qwen2-VL-7B-Instruct`**；
- **逻辑**：在 `shots.build` 之后，将每个镜头代表帧送入 Qwen2-VL，生成 30 字以内的紧凑画面特征（时间、地点、人物动作、光影色调、情绪张力），存入 `shots.captions.json`，并由 `bge-m3` 建库。

### 3. 视频修复与音频分轨扩展 (`pipeline/enhance.py` 新建)
- 封装 `demucs` 命令行，针对带有现场环境的素材一键提取 `vocals.wav` 与 `crowd.wav`；
- 封装 `real-esrgan`，针对低于 1080p 的素材提供可选的 4K 超分通道。

---

## 五、 实施计划与两阶段红线纪律

> **核心纪律：先收官，再动工。**  
> 坚决杜绝在第三期《终局葬礼》尚未落盘前分心搞大重构。现有流水线已成功跑出两期 11/11 全绿成片，完全有能力平稳拿下终局。

- **第一阶段（收官之战）**：
  1. 使用现有本地流水线，全力推进并交付《EGOIST 编年史》第三期《终局葬礼》；
  2. 实现三部曲全网发布，完成第一座标志性作品集闭环。
- **第二阶段（v2 破土动工）**：
  1. 在本地建立独立分支/目录（`v2-cloud-gpu`）；
  2. 在 AutoDL 开设 RTX 4090 镜像容器，搭建 PyTorch 2.x + CUDA 12.x 环境；
  3. 依次移植并验证：7B 流匹配配音通道、Qwen2-VL 镜头打标通道；
  4. 拿新番素材跑通全自动端到端 11/11 验证。
