# 两人工作约定

**这不是一份给外部贡献者的规范。** 这个项目现在有两个开发者，双方都在做这个产品，
不是「维护者」和「投稿人」的关系。下面每一条都是双向的，唯一的例外在第一节里明写着。

将来真有第三个人参与，再把这份拆成对外的贡献指南和对内的工作约定。现在拆是提前优化。

技术上的工程约定在 [CLAUDE.md](CLAUDE.md)，开发顺序在 [docs/ROADMAP.md](docs/ROADMAP.md)，
本文件只管**两个人之间怎么运作**。

---

## 一、决策权跟着验证能力走

两个人的项目没有仲裁人，deadlock 会拖死进度。所以决策规则要先定，而且不能定成
「谁的仓库谁说了算」——那既不服人也不解决问题。

**规则：谁能验证一件事，谁就对那件事有决定权。**

| 领域 | 谁定 | 因为 |
|---|---|---|
| 判据、阈值、门禁 | macOS 侧 | 依据来自本机实测和账号数据，且只有他能看着真实成片判好坏 |
| 内容方向、选题、发布 | macOS 侧 | 账号是他的，风险也是他的 |
| Windows / CUDA 上的一切 | Windows 侧 | 另一方**根本没法验**，二手意见没有分量 |
| 平台无关的代码结构 | 谁写谁定 | 可逆，改回来很便宜 |
| 数据格式、模块边界、重依赖 | **两人一致** | 难以回头，写 ADR |

**拍板的一方有义务把理由写下来。写不出理由的拍板不算数。** 这条对双方同样成立。

「两人一致」那一栏谈不拢时的做法：不投票（两个人投票没有意义），
而是**把分歧写成两份 ADR 草案，各写各的理由，隔一天再看**。
多数分歧在写理由的过程中就自己消失了。

---

## 二、谁能跑到哪一层

这个项目至今只在 Apple Silicon 上完整跑过。分层如下：

| 层 | 内容 | macOS | Windows / Linux |
|---|---|---|---|
| A | `pytest`，165 条，约 2.5 秒 | ✅ | **应该可以**——mlx 的 import 全在函数内部（`tts.py:158`、`asr.py:53`、`ingest.py:256`），测试里没有 POSIX 假设 |
| B | 索引、排片、渲染、质检、封面 | ✅ | 需要 ffmpeg；字体改 `config/project.json` 的 `subtitle.font` |
| C | TTS 配音、ASR 回读 | ✅ | ❌ mlx 装不上。解绑是阶段 1 的正事，见 ROADMAP |
| — | `preflight.sh` | ✅ | bash 脚本，用 WSL / Git Bash 或跳过 |

> A 层那句「应该可以」是**推断不是实测**。第一个 commit 就该去证伪它，
> 跑不过不是环境问题，是找到了第一个可移植性 bug。

**素材各用各的。** 片源不外传（CLAUDE.md「版权与发布纪律」），仓库里没有任何媒体文件。
跨平台对齐靠公开 fixture，见第五节。

---

## 三、分工

### 按平台

| | macOS / Apple Silicon | Windows / CUDA |
|---|---|---|
| 独占能力 | mlx TTS、真实出片、端到端验证 | CUDA 推理、Windows 侧一切 |
| 主要负责 | 判据、内容、每期产出 | 平台无关层、GPU 推理、泛化 |

这么切的依据是硬件，不是资历。**两边各有一件对方做不了的事**——这是好事，
它逼着接口必须谈清楚，也让第一节那条决策规则有落点。

### 按模块

同时改一个文件是两人项目最常见的事故。所以：

- **新增能力一律开新文件。** 例：视觉索引写 `pipeline/vindex.py`，不改 `subindex.py`
- **要改对方负责的模块，先在 issue 里说**——不是请示，是防止两边同时改
- **接口先约定再实现。** 谁调谁、传什么、返回什么、失败怎么表示，
  写在 issue 里达成一致再各写各的

当前模块归属见 issue 区置顶，变了在那里改。

---

## 四、共同的工程纪律

这些不是谁对谁的要求，是两个人都遵守的东西。

### 先读这五节

`CLAUDE.md` 有 35KB，一次读完记不住。**这个项目的知识不在代码里，在这几节里**：

| 节 | 为什么 |
|---|---|
| 硬约束 | 每期人类 ≤10 分钟；超预算算系统缺陷，不算加班 |
| 两层流程 | Phase 0 与每期循环。混淆这两层是本项目最容易犯的错 |
| 质检门禁 → 门禁的失败必须指向真实缺陷 | 四个真实案例，讲清「判据」在这里指什么 |
| 检索落空的处理：查询阶梯 | 「一个量能用来卡门槛，不代表它能用来排序」，项目里出现过三次 |
| 写测试的两条纪律 | 每个 PR 都按这两条验 |

读完能回答一个问题就算过：**为什么这个项目宁可硬失败，也不自动降级？**

### 合并判据

任何一方的改动都要满足：

- [ ] `pytest` 全绿（三平台 CI，见第五节）
- [ ] 新行为带**纯函数测试**，且做过变异检验（把被测行为改坏，确认测试变红）
- [ ] 没有 mock 掉 ffmpeg 或模型
- [ ] 涉及媒体产物的改动，**由能真跑的一方跑过真实数据**才合

最后一条意味着这类改动会慢，在 issue 阶段就说清要不要真跑，好安排。

### 明确不接受的东西

已经付过代价的决定，要改先去看 CLAUDE.md 里对应的理由：

- 引入 MoviePy、Remotion、任何 NLE 或视频框架
- 引入数据库、编排框架、web 界面（API 接入是阶段 2 的正事，与此不冲突）
- 为提覆盖率去 mock ffmpeg 或模型
- 检索落空时自动降级（塞空镜、塞 OP、塞纯文字）
- 把审美判断包装成机器判据（例：给封面候选打分排名）
- **放宽阈值让检查通过，而不查它为什么不通过**

最后一条最重要。发现门禁误报时**回到成因，不要调阈值**——
CLAUDE.md「门禁的失败必须指向真实缺陷」记了四个实例，四次修法都是改成因。

---

## 五、怎么协作

### 先开 issue，再写代码

**不是请示，是防撞车。** 两个人各自开工前在 issue 里写三件事：

1. 要解决什么问题（不是「要加什么功能」）
2. **判据是什么**——做完之后靠什么判断它真的对了
3. 打算怎么验：哪些测试、要不要真跑

对方回一句再动手。判据写不出来说明这事还没想清楚，那就在 issue 里一起想——
这本身就是有价值的工作，这个项目拒绝过好几个「能跑但判据是假的」方案。

### 分支与 PR

```
feat/<英文>   fix/<英文>   docs/<英文>
```

- 从 `main` 开分支，两边都不直接推 `main`
- **一个 PR 只做一件事**，正文超 400 行就拆。双方每周时间都有限，
  大 PR 会躺很久，躺久了人就凉了
- PR 里写：对应哪个 issue、判据、怎么验的、**哪些情况没覆盖**
- squash merge，commit message 中文，格式跟现有历史一致

### 跨平台对齐

两个机制：

**1. 三平台 CI —— 已建，见 `.github/workflows/test.yml`**

每次 push 与 PR，在 ubuntu / macos / windows 上跑那 165 条测试（下界 3.12 跑三平台，
上界 3.14 跑 ubuntu）。仓库公开，Actions 免费，**谁都不用为了验另一个平台买第二台电脑。**

覆盖边界写在 workflow 的注释里，简单说：A 层全覆盖（含「依赖能否全新装上」，
这正是 `numpy<2.5` 那个坑的形状）；模型推理、端到端出片、片源与索引都不覆盖——
runner 没有 CUDA 也没有 MPS。

**2. 公开 fixture —— 待建**

片源不外传，所以两边没有共同素材，也就无从验证「算出来的是不是同一个东西」。
计划用可合法共享的素材做基准（Blender 开源电影，CC-BY；*Sintel* 有稳定主角、
有官方多语字幕，两层索引都能测），仓库只提交 URL + 校验和 + 参数 + 期望输出，
不提交媒体文件。

**现在还不存在。** 比什么、期望输出长什么样，要等视觉索引有雏形才定得下来，
所以它在阶段 1 里边做边定，不在这里假装已经有了。

> 在 fixture 建起来之前，跨平台对齐只有 A 层是机器保的，B / C 层靠人对数字。
> 这是当前状态，不是设计意图。

---

## 六、红线

双方都不做：

- 提交 `*.local.md`、任何媒体文件、片源、字幕
- `git push --force`，或任何改写已推送历史的操作
- 在 issue、PR、commit、注释里贴账号数据、播放数、密钥

`.gitignore` 拦了媒体和 `*.local.md`，但**不要依赖它兜底**——`git add -f` 一样能绕过去。

---

## 七、互相的承诺

对等，两边都填：

| | macOS 侧 | Windows 侧 |
|---|---|---|
| issue 回复 | 〔__〕天内 | 〔__〕天内 |
| PR review | 〔__〕天内 | 〔__〕天内 |
| 每周投入 | 〔__〕小时 | 〔__〕小时 |

超时可以直接催，催不是冒犯。

另外两件要说在前面：

- 这个项目**目前零收入，也没打算靠它挣钱**。将来若产生收益，怎么分提前谈清楚
- 谁要退出、要降投入、要停一段，**提前说**。挂着不说比退出更伤

---

## 八、现在做什么

见 [docs/ROADMAP.md](docs/ROADMAP.md)。当前在**阶段 1：Windows 上跑通九步**，
完成判据是「在 Windows 上用自己的素材跑完九步，11 项质检全过，出一条能发的片」。
