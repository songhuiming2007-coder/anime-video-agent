# CLAUDE.local.md — 本机与本账号的具体情况

**把这个文件复制成 `CLAUDE.local.md` 再填。** `CLAUDE.local.md` 在 `.gitignore` 里，不进 git。

`CLAUDE.md` 里是任何人拿这个仓库都适用的工程约定；这里是只对**你这台机器、你这个账号**
成立的事实。两者分开的理由很实际：约定可以开源，账号数据不该开源，
而混在一起会让读的人分不清哪条是原则、哪条是「他家的情况」。

`CLAUDE.md` 有几处会指到本文件——存储位置、播放数据、标题实例、优先级。
不填也能跑，只是那几处会指向空气。

## 本机

| | |
|---|---|
| 机器 | 例：MacBook Air，Apple Silicon，16G |
| 系统盘余量 | 例：约 53G。**这条决定了大件能不能放系统盘** |
| `data/` 在哪 | 例：`data -> /Volumes/<你的卷>/anime-video-data`；或系统盘上的实体目录 |
| HF 缓存 | `data/models/hub/`（由 `pipeline/paths.py` 钉住 `HF_HOME`） |

系统盘装得下就不用外置盘，`./pipeline/preflight.sh --init` 直接建实体目录。
用外置盘的话，记一句**盘没插时会发生什么**——如果你把 `~/.cache/huggingface`
也软链到外置盘，那么盘没插时机器上任何用 HuggingFace 的工具都会报错。
那是有意的取舍（宁可当场失败也不要静默往系统盘灌几个 G），但要写下来，
不然三个月后会以为是环境坏了。撤销方法也写在这里。

## 本项目在我这里的优先级

**写清楚它是不是你的本职工作。** 这一条会直接影响 agent 在会话里怎么替你做取舍：
知道它是副业实验，agent 才会主动否掉「再打磨一下画质」这类提议。

同时定下**止损线**：总投入上限、周投入上限、到期没跑通端到端就停。
实验要有止损线，不然它会变成无底洞。

## 账号实测数据

`CLAUDE.md`「内容参数」那几条规则的依据。**没有自己的数据就别照抄别人的**——
那几条规则（抖音优先、16:9、2–4 分钟、情感向优于热血向）是某一个账号的复盘结论，
换账号、换平台、换粉丝量级都可能不成立。

建议记的维度：

| 观察 | 怎么记 |
|---|---|
| 平台差 | 同一条内容在各平台的播放比 |
| 题材 | 每部番的播放，以及它的大众认知度——这两者不一定正相关 |
| 垂直度 | 偏离账号标签的那几条掉到了什么程度 |
| 时长 | 播放最高与垫底各是多长 |
| 画幅 | 横屏 / 竖屏各自最好的一条 |
| 标题 | 播放最高与垫底的**原文**，用来校准「金句式 vs 论文式」 |
| BGM 版权 | 用了番剧原声的条数，有没有下架 / 静音替换 / 限流 |

**这些数字会过时。** 每次复盘拉新数据回来，如果哪条规则被推翻了，
**先改 `CLAUDE.md` 里那条规则，再改实践**——不要反过来。

## 每期实际耗时

记在各期的 `meta.md` 里，这里只记规矩：连续 N 期超预算 → 停止产出，回头修流水线。
**超预算是系统缺陷，不是加班的理由。**
