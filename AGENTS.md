# AGENTS.md — anime-video-agent

本文件是**常驻规则**：每次会话载入，只放每时每刻生效的约定。长文件没人读得完会失权威，本机事实**不进 git**。

## 文档地图

> **开工第一铁律**：`python -m pipeline.status data/episodes/<期号>`，依据输出只读 **≤100 行手册 `docs/WORKFLOW.md`**。

| 文件 | 记什么 | 什么时候读 |
|---|---|---|
| **本文件** | 常驻规则：定位、硬约束、判据、红线、命令 | 每次会话 |
| `docs/WORKFLOW.md` | 【生产态】每期九步速查与 4 大停机规程（≤100 行精炼版） | 跑视频作业时必读 |
| `docs/runbook/` | 【生产态】分工序独立 SOP（01 到 09，每篇独立成册、按需取用） | 跑具体某一步骤时按需读取 |
| `docs/INDEX.md` | **文档总入口**：开发态与生产态清晰隔离导航 | 查阅文档总貌时 |
| `docs/dev/STANDARD.md` | 【开发态】判据系统定义：每条标准的「为什么」与违反程序 | 做取舍 / 写判据 / 改标准时 |
| `docs/dev/postmortems/` | 【开发态】历史踩坑论证、演化背景与全量复盘 | 探究历史成因或修改机制时 |
| `docs/dev/ROADMAP.md` | 【开发态】开发顺序与阶段闸门 | 决定下一步系统研发方向时 |
| `docs/dev/issues/` | 【开发态】未解决问题单表 | 找当前系统卡点时 |
| `docs/dev/plans/` | 【开发态】可执行施工图索引 | 要动工改代码实现时 |
| `docs/dev/adr/` | 【开发态】难以回头的架构决策与证据 | 动某块系统架构前先读对应 ADR |
| `skills/write-script/` | 写稿指令：五种题材骨架与判据 | 第 02 步写稿时 |
| `README.md` | 全流程介绍与快速上手 | 给不了解项目的人 |

## 一、定位

用 agent 流水线批量生产动漫二创视频。**实验项目，优先级低于主线。**

**成功标准是流水线本身**，不是内容表现：交付物 = 一套可复用的 agent 工程系统；内容数据（播放、涨粉）= 验证信号。任何取舍都问：这件事让**流水线**更好，还是只让**这一期视频**更好？后者一律降优先级。

**可复用性总纲：这个项目不是为一部动漫写的，是为 1 亿部写的。** 机制进代码、内容进配置；判断是机制还是内容，问「换一部番它变不变」——变就是内容，不许写死。换番只动 config 与 Phase 0 资产，不改 `pipeline/`；阈值逐番标定，不许抄默认值；索引按番隔离，跨番命中是静默失败。**任何「换一部番就要改代码」的地方都是缺陷，不是惯例。** 完整七条见 `docs/dev/STANDARD.md` 第二节。

**不做的事**（理由见 `docs/dev/STANDARD.md` 第一节）：纯搬运、自动上传、无限打磨、政治议题进自动管道、引入 NLE/视频框架（MoviePy、Remotion）/数据库/编排/web（指外部编排框架与 web server 层；harness 事件层与 Electron 桌面端已由 ADR-0020 解禁）、mock ffmpeg 或模型、检索落空自动降级、把审美判断包装成机器判据、调阈值绕过门禁。

## 二、硬约束（违反即视为系统缺陷）

- **生产期代码写保护（Code Freeze，严禁不声不响改代码）**：
  在进行做视频（写稿、配音、排片、渲染、封面等）任务期间，`pipeline/`、`config/`、`tests/` 下的代码与配置视为**绝对只读（Read-Only）**。
  遇到任何异常，99% 是由于当期 `01-topic.md` 或 `02-script.md` 字段格式错误、语法不符或素材缺失引起的，**只允许排查和修改当期目录下的输入文件**。
  严禁 Agent 私自修改 `pipeline/` 源码添加单期特判造屎山。若确信为通用流水线代码 Bug 必须修改，**必须立即停机向人类出示三项汇报**：
  1. 原始报错堆栈；
  2. 证明为什么这是「引擎缺陷」而不是「当期输入写错」；
  3. 拟修改的最小 diff 方案与对其他番剧的影响。
  **未获得人类明确回复「允许改代码」前，绝对禁止改动任何一行 Python 源码。**
- **每期人类投入是观测量，不设硬预算**（2026-09-23 用户裁决）：原「≤10 分钟」被约 20 期实践否证（20 分钟的片光 03.5 就 ≥30 分钟），2026-09-19 改为 `k × 片长`；现进一步降为观测——墙钟计时混入注意力因素，不宜当门槛。`human_time.json` 继续记录，用来找最费人的环节，再改流程或砍环节；不再有「超预算即缺陷」「连续 3 期停产」判据。
- **禁止无限打磨。** 画质、转场、文案润色边际收益极低；时间只花在更自动、更可靠上。
- 建设期止损线（3 周 / 每周 ≤ 6 小时）**已满足解除**；人时预算于 2026-09-23 降为观测量，目前不设止损线。

## 三、判据（本项目的灵魂，全部硬标准）

完整证据与踩坑实例见 `docs/dev/STANDARD.md` 第三、四节。核心判据：

1. **每加一道检查，先问它测的是不是真实产物。** 测的不是真实产物，通过就毫无意义（字幕项曾算「原文长度÷每行容量」，而 libass 对无空格的中文根本不折行）。
2. **一个量能用来卡门槛，不代表它能用来排序。** 同一教训踩过四次：拉普拉斯方差给封面打分、回读 CER 判轴、查询阶梯取最高分、跨通道分数融合。
3. **门禁失败回到成因，不调阈值。** 门禁只有两种坏法：该拦的没拦、拦了不该拦的。调阈值是拿一个真问题换一堆假问题。
4. **门禁拿不到能证伪的信息时，跳过样本，不定罪。** 把「我不知道」记成「不合格」，是让人干脆关掉门禁的最快方式。
5. **先归一化再比对的检查，问被归一化掉的部分是不是缺陷所在。** 标点既是噪声（ASR 不还原）又是信号（TTS 不该念出）。
6. **判据要么被执行，要么被删除。** 留着不执行的条款会让整份文档失去权威。
7. **凡要写进配置 / 代码的判断，不许带「可能」。** 验不了就先不写（`numpy<2.5` 那次上界写错地方的教训）。
8. **素材边界的判据是「集数对得上」，不是「检索得到东西」。** 后者永远为真。
9. **跳过不是通过。** 缺前置文件跑不了的检查必须显式列出，退出码也算不合格。
10. **分数只在它自己的通道内排序，绝不跨通道比较。** 台词的 0.7 和画面的 0.7 长得像纯属巧合。
    **唯一例外：presence 在场分**（ADR-0004）——它只在台词通道内、同一段落、
    台词分差 ≤ `PRESENCE_BAND` 的同一档候选间做 tie-break，绝不跨段落、绝不跨通道、
    绝无复合分；这个例外本身就是判据的落实：排序轴仍是台词分，presence 只细化它。
11. **对抗审查必须兼顾正确性与厚度密度，审查报告必须含通过率抽样（2026-09-03 升级）。**
    看过笔记的 agent 已被笔记的因果框架带走，找得出「顺不顺」、找不出「笔记与事实的分歧」。
    审查维度必须包含双重指标：① 正确性（猎杀编造、因果错、集数错、时间码漂移）；② 厚度与密度
    （严禁单行表格化摘要降级，单集场景数 $\ge 4$ 个、行级台词引用 $\ge 2$ 处，未达标直接判定为「严重写薄」当场打回）。
    对抗审查报告必须含**随机抽样通过清单**——只报错不报过的报告是选择偏差，无法校准整体可信度
    （2026-08-13 罪恶王冠实测：45 条抽样通过 + 14 条问题）。对抗审查后必须有
    **终审裁决**：逐条采纳/驳回（驳回写理由）、修正回写笔记、裁决与报告存档——
    没有终审的审计等于白审。
12. **引文时间码必须行级对准。** 指到台词所在行，不是场景起点。实测 6 处时间码偏差
    全部是「场景起点 vs 台词行」的差（3-21 秒），写稿 agent 按错码检索会命中相邻的
    另一句台词。
13. **切分标定：过切无害、漏切有害，拿不准往低取。** 依据 ADR-0003 与三番标定实录
    （证据在 `config/project.json` 的 `_scene_threshold_note`）。

## 四、失败处理

- **诚实失败优于凑合交付。** 拿不到关键素材、配音反复不自然、检索无匹配——立即停下报告，不许降低标准硬交。这是本项目最重要的一条。
- **源抓不到 ≠ 放弃，严禁偷懒降级为水百科（2026-09-03 立）。** 主 agent 或 subagent 调研剧情、考据论坛长评（Bangumi、知乎、贴吧、B站专栏等）时遇反爬（403、Cloudflare、验证码、JS 渲染阻断）是常态不是意外。**严禁因静态获取失败就偷懒退回简略百科交差**；必须执行仓内工具升级链：`web_fetch` 静态抓取失败 → 升级 `crawl`（无头渲染，遇反爬开 stealth 绕盾）；确需登录态或复杂交互 → 升级 `browser`（独立持久化 profile、逐调用弹人审卡），严禁直连主力 Chrome Default 配置。宁可多花 10 分钟提取一手深度文本，绝不拿二手粗糙概括充数。
- **不许硬塞不匹配的画面，留白远优于画文不符。**
- **检索落空走查询阶梯**：`查询` → `备选` → `配音` 原文，逐级重试，门槛 `0.45` 不动。**只救不比**——上一级够格就到此为止，绝不因下一级分数更高而换。加大 TOPK 救不了落空（结果按分数降序，top-1 不及格时第 25 名只会更低）。
- **三级都不及格 → `"status": "no_match"`，渲染硬失败交人处理，不自动降级。** 落空绝大多数是查询写错了：写台词语义，别向字幕索引要画面构图。
- **成片交人前至少听一遍开头和最长的那段。** 十一项质检全绿不代表能听（归一化会抹掉证据）。

## 五、工程约定

- **产物即状态**：每阶段产物落盘；人机交接走文件，不走对话上下文。任何一步失败可从中间接上重跑。
- **每个常量写依据**：「为什么是这个数」进代码/配置，「不这么做会怎样」写进 workaround。
- **路径一律相对仓库根**（`./data/...`），不硬编码卷路径。
- **依赖隔离**：依赖装项目内（uv 虚拟环境），不污染全局。新增能力开新文件。
- **存储分离**：仓库只有纯文本进 git；大件在 `data/` **不进 git**，可以是外置盘符号链接。
- **脚本绝不自动创建 `data/`**：不可达立刻报错退出。建骨架走 `./pipeline/preflight.sh --init`。
- **HF 缓存钉到 SSD**：本项目在 `data/models/hub/`；`from . import paths` 必须排在第三方 import 之前。
- **素材来源硬规则**：字幕必须照手上视频打（内封/ASR/已对轴外挂）；片源完整性以 ffmpeg 解复用为准；Phase 0 一次做完全集。

## 六、每期九步

产物即状态，看目录文件知进度。顺序不可逆。人类在 02.5（审改）/ 03.5（顺听，错字走 corrections.json -> --apply-patch）/ 05 / 09 出现。详见 `docs/WORKFLOW.md`。

| 步 | 01 选题 | 02 写稿 | 03 配音 | 04 排片 | 05 审时间码(人) | 06 渲染 | 07 质检 | 08 封面标题 | 09 发布(人) |
|---|---|---|---|---|---|---|---|---|---|
| 产物 | `01-topic.md` | `02-script.md` | `03-audio/` | `04-clips.json` | `04-clips.approved.json` | `05-final.mp4` | `06-check.log` | `07-cover/`+`07-titles.md` | — |

- **01 必须带 `类型`**：骨架由题材决定；`模式` 与 `张力` 杂谈必填。
- **02 `查询` 是机器接口**：送进索引检索台词语义；`集` 字段做集内掩码。先保真再去味。
- **03 按句合成**：时长 `ffprobe` 复核进 `manifest.json`，不采信模型自报。
- **05 人工关卡**：`--approve` 必须是显式动作。
- **04 视觉通道二选一**：`人物` 走台词检索+在场过滤；`场景` 走画面语义检索。不跨通道比分。
- **08 候选不排名**：9 张候选机器筛+人选；封面硬过滤，排片软过滤。

## 七、环境与验证

- Python 3.12 / 3.13 / 3.14，用 uv 建虚拟环境，不依赖系统解释器。`pyproject.toml` 的 **`numpy>=2.0,<2.5` 上界不能删**（依赖链 mlx-whisper → numba → llvmlite，numpy 2.5 会回退到装不上的组合；此坑**只咬新环境**，所以「我这儿能跑」不构成证据）。
- ffmpeg 必须带 libass（`preflight.sh` 查）。中文字幕字体 `Hiragino Sans GB`（`config/project.json` 里配置）。

```bash
pytest                                          # 纯函数测试，约 30 秒（1700+ 条），改完代码先跑
./pipeline/preflight.sh                         # 环境自检：data 可达 + ffmpeg 带 libass
python -m pipeline.check_script <02-script.md>  # 稿件机检
python -m pipeline.vindex status --anime <番>   # 七条数字对不对得上（v2 起含 captions）
```

**写测试两条纪律**：① **期望值先在实现上跑一遍再写进断言**（不是「跑出什么断言什么」——先看懂为什么是这个值，说不通就是找到 bug 了）；② **写完做变异检验**——把被测行为故意改坏，看测试红不红。一条永远绿的断言和没有断言效果一样，只会让人以为这块被覆盖了（又一种静默失败）。
